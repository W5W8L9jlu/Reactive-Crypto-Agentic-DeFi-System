# Project-scoped Codex config example
# Adjust model/provider/approval policy to your environment.

[session]
# Keep project sessions deterministic and reviewable.
auto_approve_edits = false

[context]
# Prefer repo-local guidance and prompt files.
include_git_status = true

[tools]
shell = true
