# W4 Wave Gate

- Wave: `W4`
- Gate owner: `Codex`
- Date: `2026-04-08`
- Branch / HEAD observed: `codex/wave4` @ `9b85ce5`
- Working tree state: `dirty (untracked: W4.wave_integration_matrix.md, W4.wave_risk_register.md)`
- Gate conclusion: `PASS_WITH_NOTES`
- Next wave may start: `Yes (must-fix items must be treated as next-wave entry blockers)`

## A. Evidence Reviewed
- Wave docs:
  - `docs/acceptance/waves/W4.wave_integration_matrix.md`
  - `docs/acceptance/waves/W4.wave_risk_register.md`
- Thread acceptance docs (wave4):
  - `docs/acceptance/threads/wave4/emergency_force_close.thread_acceptance.md`
  - `docs/acceptance/threads/wave4/shadow_monitor.thread_acceptance.md`
- Thread handoff docs (wave4):
  - `docs/acceptance/handoffs/wave4/emergency_force_close.thread_to_thread_handoff.md`
  - `docs/acceptance/handoffs/wave4/shadow_monitor.thread_to_thread_handoff.md`
- Thread test evidence docs:
  - `docs/acceptance/threads/wave4/emergency_force_close.test_evidence.md`
  - `docs/acceptance/threads/wave4/shadow_monitor.test_evidence.md`
- Current integration branch evidence:
  - `git diff --name-status 4297287..9b85ce5` -> wave4 代码与验收文档进入同一提交范围
  - `git diff --stat 4297287..9b85ce5` -> `20 files changed, 1149 insertions(+)`
  - `git status --short --branch` -> 当前仅 wave 文档为未跟踪变更
- Commands rerun during this gate:
  - `forge test --root backend/contracts --contracts backend/contracts/core -vv` -> `Ran 8 tests ... 8 passed`
  - `python -m py_compile backend/monitor/shadow_monitor.py backend/monitor/reconciliation_daemon.py` -> success (exit 0)

## B. Thread Status

| Thread | Acceptance doc | Handoff doc | Current repo evidence | Gate read |
| --- | --- | --- | --- | --- |
| `emergency_force_close` | exists | exists | `ReactiveInvestmentCompiler.sol` 已含 break-glass 接口、权限控制、事件；基础合约回归 8/8 通过；专项 emergency 用例未给出 | `delivered with notes (not frozen as complete integration)` |
| `shadow_monitor` | exists | exists | `backend/monitor/{shadow_monitor.py,reconciliation_daemon.py}` 已交付最小守护闭环；`py_compile` 通过；跨线程联调未给出 | `delivered with notes (interface-level complete, integration incomplete)` |

## C. Gate Criteria Check
- All threads delivered or clearly marked incomplete: `yes`
  - 两个线程文档都明确写出 `IMPLEMENTED_IN_WORKTREE*` 且列出 `not verified yet` 缺口
- Thread acceptance docs exist: `yes`
- Thread handoff docs exist: `yes`
- Integration mismatches are recorded: `yes`
  - 已在 `W4.wave_integration_matrix.md` 与 `W4.wave_risk_register.md` 记录 payload/interface mismatch
- Minimum happy path is verified or explicitly unverified: `yes`
  - 已验证：`ReactiveInvestmentCompiler` 现有主路径回归测试通过；`shadow_monitor` 模块可编译且线程证据含 3 场景断言
  - 明确未验证：`shadow_monitor -> emergency_force_close` 端到端链路、emergency 专项测试、`shadow_monitor -> cli_surface` 实链路映射
- Known issues separated into must-fix vs carry-over: `yes`

## D. Why This Gate Is `PASS_WITH_NOTES` (Not `PASS`)
- wave4 模块交付和文档交付完整，但跨线程关键联动仍缺实证（monitor recommendation 不能直接形成合约可执行参数）。
- 已存在明确接口漂移（`IReactiveInvestmentCompiler` 未声明 emergency 新接口）。
- 已验证项主要在模块/接口层，不足以支撑“集成成功”的 `PASS`。

## E. Must-Fix Items
- 冻结并实现 `ForceCloseRecommendation -> emergencyForceClose` 适配契约：至少包含 `intent_id(str)->intentId(bytes32)` 编码规则与 `maxSlippageBps` 来源。
- 修复 `IReactiveInvestmentCompiler` 接口漂移（补齐 emergency API 或定义并切换到独立 emergency interface）。
- 提供至少 1 条跨线程最小联调证据：`shadow_monitor` critical 告警 -> 可执行 emergency 调用参数 -> 合约返回/事件校验。
- 补齐 emergency 专项最小验证（权限、非 ActivePosition 拒绝、force-close 后迟滞回调）。
- 统一回填 wave4 线程文档锚点（当前文档多处仍写 `HEAD=4297287`，与实际 `9b85ce5` 不一致）。

## F. Allowed Carry-Over Issues
- `estimated_additional_loss_usd` 仍为临时公式（`TODO(domain)`），允许延后到公式契约冻结波次。
- `shadow_monitor -> cli_surface AlertView` 实际字段映射可延后，但需在首次接入 CLI 告警链路前补证。
- timestamp 单位统一策略（timezone-aware datetime 与其他线程时间字段）可延后，但需在首条跨线程时间判断路径前冻结。
- 线程负责人字段仍为 `not verified yet`。

## G. Gate Result
- Status: `PASS_WITH_NOTES`
- Next wave may start: `Yes`
- Must-fix items: see section `E`
- Allowed carry-over issues: see section `F`
