# W2 to W3 Wave Handoff

- Current Wave: `W2`
- Next Wave: `W3`
- Date: `2026-04-07`
- Gate conclusion: `PASS`
- Tag: `W2-freeze-20260407`
- Working tree: `clean`
- Sources:
  - Gate: [W2.wave_gate.md](file:///d:/reactive-crypto-agentic-DeFi-system/docs/acceptance/waves/W2.wave_gate.md)
  - Integration Matrix: [W2.wave_integration_matrix.md](file:///d:/reactive-crypto-agentic-DeFi-system/docs/acceptance/waves/W2.wave_integration_matrix.md)
  - Risk Register: [W2.wave_risk_register.md](file:///d:/reactive-crypto-agentic-DeFi-system/docs/acceptance/waves/W2.wave_risk_register.md)
  - Exit Report: [W2.wave_exit_report.md](file:///d:/reactive-crypto-agentic-DeFi-system/docs/acceptance/waves/W2.wave_exit_report.md)
  - Module handoffs (wave2): [approval_flow](file:///d:/reactive-crypto-agentic-DeFi-system/docs/acceptance/handoffs/wave2/approval_flow.thread_to_thread_handoff.md), [decision_context_builder](file:///d:/reactive-crypto-agentic-DeFi-system/docs/acceptance/handoffs/wave2/decision_context_builder.thread_to_thread_handoff.md), [execution_compiler](file:///d:/reactive-crypto-agentic-DeFi-system/docs/acceptance/handoffs/wave2/execution_compiler.thread_to_thread_handoff.md), [pre_registration_check](file:///d:/reactive-crypto-agentic-DeFi-system/docs/acceptance/handoffs/wave2/pre_registration_check.thread_to_thread_handoff.md)
  - Current integration code: compiler [compiler.py](file:///d:/reactive-crypto-agentic-DeFi-system/backend/execution/compiler/compiler.py), compiler models [models.py](file:///d:/reactive-crypto-agentic-DeFi-system/backend/execution/compiler/models.py), validation [validation_engine.py](file:///d:/reactive-crypto-agentic-DeFi-system/backend/validation/validation_engine.py), export [export_outputs.py](file:///d:/reactive-crypto-agentic-DeFi-system/backend/export/export_outputs.py), approval [flow.py](file:///d:/reactive-crypto-agentic-DeFi-system/backend/cli/approval/flow.py)

## Frozen capabilities from this wave

- Execution compiler (registration-time compiler) is frozen with test-backed entrypoints and payloads:
  - Entrypoints: `compile_execution_plan(...)`, `freeze_contract_call_inputs(...)` ([compiler.py](file:///d:/reactive-crypto-agentic-DeFi-system/backend/execution/compiler/compiler.py#L9-L73))
  - Inputs: `CompilationContext`, `RegistrationContext`, `ChainStateSnapshot`, `CompilerConfig` ([models.py](file:///d:/reactive-crypto-agentic-DeFi-system/backend/execution/compiler/models.py))
  - Outputs: `ExecutionPlan`, `RegisterPayload`, `ContractRegisterCallInputs`, `ContractInvestmentIntent` ([models.py](file:///d:/reactive-crypto-agentic-DeFi-system/backend/execution/compiler/models.py))
  - Alias mapping and units are verified in tests: `intentId`, `owner`, `inputToken`, `outputToken`, `plannedEntrySize`, `entryAmountOutMinimum`, `entryValidUntil`, `maxGasPriceGwei`, `stopLossSlippageBps`, `takeProfitSlippageBps`, `exitMinOutFloor`; TTL in seconds, BPS in integer bps, gas in gwei.
- Validation engine is frozen as a consumer of the compiler:
  - Entrypoints: `validate_inputs(...)`, `validate_inputs_or_raise(...)` ([validation_engine.py](file:///d:/reactive-crypto-agentic-DeFi-system/backend/validation/validation_engine.py#L13-L54))
  - Preserves contract bindings for identity and register-call fields; raises explicit errors for undefined allowlists.
- Export outputs is frozen at the envelope boundary:
  - Entrypoint: `export_outputs(...)` ([export_outputs.py](file:///d:/reactive-crypto-agentic-DeFi-system/backend/export/export_outputs.py#L41-L81))
  - Envelopes: `DecisionArtifact`, `ExecutionRecord`, `MachineTruth`, `ExportOutputs`
  - Frozen three-output bundle: `machine_truth_json`, `audit_markdown`, `investment_memo` (inner payloads remain dict-root; only the envelope and happy-path preservation are frozen).
- Typed approval flow entry is frozen for consumption:
  - Entrypoints: `build_approval_battle_card(...)`, `show_approval(..., raw=False)`, `approve_intent(...)`, `reject_intent(...)` ([flow.py](file:///d:/reactive-crypto-agentic-DeFi-system/backend/cli/approval/flow.py#L41-L152))
  - Consumes `TradeIntent`, `ExecutionPlan`, `ValidationResult`, `DecisionMeta`; enforces expiry/validation gates; renders bps and display fields.
  - Caveat: TTL display currently derives from `DecisionMeta`, not compiler-tightened expiry (see remaining issues).
- Decision context builder is partially stable at the normalized fetcher seam:
  - Entrypoint: `DecisionContextBuilder.build(...) -> DecisionContext`
  - Normalized typed outputs (`MarketTrend`, `LiquidityDepth`, `OnchainFlow`, etc.) and mock-backed fetchers are proven; live provider translation is not verified.
- Pre-registration check interfaces exist and abort-path behavior is evidenced:
  - Entrypoints: `run_pre_registration_check(...)`, `run_pre_registration_check_or_raise(...)`
  - Typed inputs/outputs and explicit domain errors are present.
  - A direct frozen adapter into the compiler does not exist; sequential `precheck -> compile` is reproducible but not a frozen contract.

## Safe truths for next wave to rely on

- Registration payload alias mapping and units are stable across compiler → validation → export → contract:
  - CamelCase aliases listed above are preserved into `ContractRegisterCallInputs` and validated bindings.
  - `trade_intent.ttl_seconds` is tightened in the compiler and becomes `register_payload.entryValidUntil` (Unix timestamp seconds).
- Validation engine returns `ValidationResult` with stable contract bindings and explicit error semantics (`MissingValidationSpecError` on undefined allowlists).
- Export outputs preserves machine_truth_json as the only execution source of truth; empty or non-serializable artifacts raise explicit errors.
- Approval flow typed path is a safe entrypoint for operator actions and display; `raw=True` echoes provided machine truth verbatim.
- DecisionContext normalized shapes exist for future adapters; unit conventions align with core models.
- The baseline is anchored to tag `W2-freeze-20260407`; gate is `PASS`.

## Things the next wave must NOT assume

- Do not assume live provider payloads already align with the builder’s normalized fetcher contract; real provider translation is not verified.
- Do not assume a frozen `DecisionContext -> pre_registration_check` or `PreRegistrationCheckResult -> compiler` adapter exists.
- Do not assume the approval TTL countdown reflects compiler-tightened expiry; current display uses `DecisionMeta`.
- Do not assume the legacy mapping-style approval mapper in `backend/cli/models.py` matches the typed path; keep consumers on the typed path.
- Do not assume `DecisionArtifact` and `ExecutionRecord` have typed inner schemas; only the envelope is frozen.
- Do not assume a one-command end-to-end CLI route/adapter is already wired; not verified in W2.

## Remaining issues

- Blockers
  - None. W2 is frozen on a clean tag `W2-freeze-20260407`.
- Known gaps
  - Approval TTL truth-source mismatch (display uses `DecisionMeta` instead of compiler `entryValidUntil`).
  - Duplicate approval battle-card mappers (typed path vs legacy mapping in `backend/cli/models.py`).
  - No frozen adapter for `PreRegistrationCheckResult -> CompilationContext` (TTL buffer, gas cap, slippage authority not formalized).
  - Live provider payload translation for `decision_context_builder` is not verified.
  - Export inner payloads remain dict-root; field drift lacks type-level rejection.
  - Approval CLI route/adapter end-to-end wiring is not verified.
- Acceptable carry-over
  - Real provider integration and translation layer for the builder.
  - Downstream consumers of `DecisionContext`, including freshness/staleness semantics.
  - Dedicated automated tests for `pre_registration_check`.
  - Stricter typing or a contract test around export inner records.
  - A minimal cross-module harness tying compiler → validation → export → approval.

## Suggested next-wave start order

1. Unify approval flow on the typed path and switch TTL display to `register_payload.entry_valid_until`; remove the legacy mapper.
2. Freeze an explicit adapter contract for `PreRegistrationCheckResult -> CompilationContext`, clarifying ownership of TTL buffer, gas cap, and slippage units.
3. Add a live provider translation layer and tests to feed normalized builder inputs from at least one concrete provider.
4. Add minimal typing or a contract test for export inner records to fail on unexpected field drift.
5. Wire a CLI route/adapter that exercises `show_approval` and `approve_intent` against exported machine truth.
6. Optionally add a single-command verification harness for compiler → validation → export → approval.

## Return: most important frozen interfaces

- Compiler seam
  - `compile_execution_plan(context: CompilationContext) -> ExecutionPlan` ([compiler.py](file:///d:/reactive-crypto-agentic-DeFi-system/backend/execution/compiler/compiler.py#L9-L69))
  - `freeze_contract_call_inputs(plan: ExecutionPlan) -> ContractRegisterCallInputs` ([compiler.py](file:///d:/reactive-crypto-agentic-DeFi-system/backend/execution/compiler/compiler.py#L71-L73))
  - Models: `ExecutionPlan`, `RegisterPayload`, `ContractRegisterCallInputs`, `ContractInvestmentIntent`, `RegistrationContext`, `ChainStateSnapshot`, `CompilerConfig` ([models.py](file:///d:/reactive-crypto-agentic-DeFi-system/backend/execution/compiler/models.py))
- Validation seam
  - `validate_inputs(...)`, `validate_inputs_or_raise(...)` ([validation_engine.py](file:///d:/reactive-crypto-agentic-DeFi-system/backend/validation/validation_engine.py#L13-L54))
  - Contract bindings mapping compiler aliases to register call and investment intent fields.
- Export seam
  - `export_outputs(...)` with `DecisionArtifact`, `ExecutionRecord`, `MachineTruth`, `ExportOutputs` ([export_outputs.py](file:///d:/reactive-crypto-agentic-DeFi-system/backend/export/export_outputs.py#L41-L81))
- Approval typed entrypoints
  - `build_approval_battle_card(...)`, `show_approval(..., raw=False)`, `approve_intent(...)`, `reject_intent(...)` ([flow.py](file:///d:/reactive-crypto-agentic-DeFi-system/backend/cli/approval/flow.py#L41-L152))

## Return: biggest assumptions the next wave must avoid

- Assuming live providers already emit normalized payloads the builder expects.
- Assuming a frozen adapter exists between precheck results and the compiler’s compilation context.
- Assuming approval TTL display matches the compiler’s tightened expiry.
- Assuming the legacy approval mapper equals the typed path.
- Assuming export inner payloads are fully typed and drift-proof.
- Assuming an end-to-end CLI route is already wired and verified.

---

- Files modified: this document only.
- Invariants kept:
  - Machine Truth JSON remains the single source of execution truth.
  - Compiler operates at registration time; no calldata generation.
  - Core objects are typed with consistent units (seconds/bps/gwei).
- Validations consulted:
  - Gate/Exit/Matrix/Risk docs and current tag `W2-freeze-20260407` plus module tests and inline smoke recorded in gate docs.
- TODO / Risks:
  - Address known gaps listed above in the suggested start order; blockers are none for starting W3 on this baseline.

