# W4 Wave Exit Report

- Wave: `W4`
- Date: `2026-04-08`
- Branch / HEAD observed: `codex/wave4` @ `9b85ce5`
- Working tree state: `dirty (untracked wave docs present)`
- Exit conclusion: `PASS_WITH_NOTES`

## A. What Was Factually Verified In The Current Workspace
- `emergency_force_close` (contract baseline regression)
  - `forge test --root backend/contracts --contracts backend/contracts/core -vv`
  - Result: `Ran 8 tests ... 8 passed, 0 failed`
  - Verified scope: 现有 `ReactiveInvestmentCompiler` 主路径（register/entry/exit/closed retrigger/constraint）未回归失败。
- `shadow_monitor` (module buildability)
  - `python -m py_compile backend/monitor/shadow_monitor.py backend/monitor/reconciliation_daemon.py`
  - Result: success (exit 0)
  - Verified scope: wave4 monitor 两个核心模块在当前分支可编译加载。
- Wave integration branch diff
  - `git diff --name-status 4297287..9b85ce5`
  - `git diff --stat 4297287..9b85ce5`
  - Result: wave4 核心改动（合约 emergency + monitor 模块 + wave4 acceptance/handoff 文档）集中在 `9b85ce5` 范围内。

## B. What Remains Unverified
- `shadow_monitor -> emergency_force_close` 端到端联动（告警到合约调用）未有实测证据。
- emergency 专项最小验证（权限、非 ActivePosition、force-close 后迟滞回调）未在已跑测试集中体现。
- `shadow_monitor -> cli_surface` 实际字段映射联调未验证。
- `IReactiveInvestmentCompiler` 接口新增 emergency API 的统一声明/消费链路未验证完成。
- timestamp 与 bps/pct 跨线程统一策略未冻结（仅记录风险，暂无端到端实证）。

## C. Document Consistency Findings
- wave4 两个线程的 acceptance/handoff 文档中，仍保留 `HEAD=4297287` 与“工作树未提交”叙述。
- 当前真实分支已为 `codex/wave4@9b85ce5`，存在锚点漂移，需要后续文档回填校正。

## D. Integration Readout
- Verified seams:
  - `emergency_force_close` 模块级代码路径不变量（Closed 后普通触发会 `ClosedIntentCannotExecute`）已在实现层可见。
  - `shadow_monitor` 输入/输出模型与守护流程（grace -> critical -> recommendation）在模块设计和线程证据中清晰定义。
- Not verified seams:
  - `force_close_recommendations` 到合约调用参数的完整映射（`intent_id` 类型、`maxSlippageBps` 来源）。
  - downstream CLI 展示链路与真实 payload 对齐。
  - wave4 最小跨线程 happy path（monitor critical -> emergency tx semantics）联调。

## E. Must-Fix Before/At Next Wave Start
- 冻结并实现 `ForceCloseRecommendation -> emergencyForceClose` 适配契约（字段、类型、单位完整闭环）。
- 修复/冻结 emergency 接口声明策略（`IReactiveInvestmentCompiler` 或独立 interface）。
- 补齐 emergency 专项测试并纳入可复现命令清单。
- 产出至少 1 条跨线程联调证据，覆盖 critical 告警到 emergency 调用结果。
- 修正 wave4 线程文档锚点漂移（分支/HEAD/提交状态）。

## F. Allowed Carry-Over
- `estimated_additional_loss_usd` 正式公式冻结可延后到风险模型收敛波次。
- `shadow_monitor -> cli_surface` 视图字段映射可延后到 CLI 联调波次，但不得在未验证前标记已集成。
- timestamp 统一规范与转换测试可延后到首个依赖时间窗口的跨线程链路。
- 线程负责人字段补全可延后。

## G. Commands Run
- `git branch --show-current`
- `git rev-parse --short HEAD`
- `git status --short --branch`
- `git diff --name-status 4297287..9b85ce5`
- `git diff --stat 4297287..9b85ce5`
- `forge test --root backend/contracts --contracts backend/contracts/core -vv`
- `python -m py_compile backend/monitor/shadow_monitor.py backend/monitor/reconciliation_daemon.py`

## H. Exit Result
- Gate status: `PASS_WITH_NOTES`
- Next wave may start: `Yes`
- Must-fix items: see section `E`
- Allowed carry-over issues: see section `F`
