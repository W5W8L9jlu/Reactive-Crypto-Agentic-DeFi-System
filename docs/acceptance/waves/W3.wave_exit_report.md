# W3 Wave Exit Report

- Wave: `W3`
- Date: `2026-04-08`
- Branch / HEAD observed: `codex/w3-baseline` @ `817c32f`
- Working tree state: `dirty (untracked wave docs present)`
- Exit conclusion: `PASS_WITH_NOTES`

## A. What Was Factually Verified In The Current Workspace
- `cli_surface`
  - `python -m unittest backend.cli.test_app -v`
  - Result: `Ran 4 tests ... OK`
  - Verified scope: 六组命令路由装配、`approval show --raw` 参数约束与错误码行为。
- `cli approval flow`
  - `python -m unittest backend.cli.approval.test_approval_flow -v`
  - Result: `Ran 5 tests ... OK`
  - Verified scope: battle-card 渲染、raw 展示、approve/reject 分支、过期阻断。
- `cli alert view`
  - `python -m unittest backend.cli.views.test_alerts -v`
  - Result: `Ran 2 tests ... OK`
  - Verified scope: critical/warning 排序、空告警渲染。
- `reactive_runtime`
  - `$env:PYTHONPATH='.'; python -m unittest backend.reactive.adapters.test_reactive_runtime -v`
  - Result: `Ran 5 tests ... OK`
  - Verified scope: `entry/stop_loss/take_profit` 触发路径、callback 类型校验、状态迁移校验。
- branch integration diff (recent wave slice)
  - `git diff --name-status HEAD~3..HEAD`
  - `git diff --stat HEAD~3..HEAD`
  - Result: `backend/cli/*` 与 `backend/reactive/adapters/*` 及 wave3 acceptance/handoff 文档已进入近期提交范围。

## B. What Remains Unverified
- `cli_surface -> cryptoagents_adapter/execution_layer/reactive_runtime` 真实 adapter 链路。
- `reactive_runtime -> execution_layer` tx receipt/log 串联与失败结构对齐。
- `machine_truth_json`（CLI raw）与真实 `export_outputs` 结构化 payload 对齐。
- `AlertView` 与真实 `shadow_monitor` 输出字段映射。
- timestamp 跨线程单位统一（UTC datetime vs epoch seconds）。
- bps/pct 跨线程单位统一（本波次尚无实测链路）。

## C. Document Consistency Findings
- 存在锚点漂移：
  - `reactive_runtime` 线程文档仍记录旧分支/旧 HEAD（`w3-reactive-runtime` / `b920945`）；
  - `cli_surface` 线程文档也记录旧分支描述（commit `9177017` 可核实，但分支状态叙述过时）。
- 影响：降低 gate 追溯一致性，容易误判“当前基线”。

## D. Integration Readout
- Verified seams:
  - `user command -> cli_surface route handlers`（route-contract 级）
  - `cli_surface approval.show(raw) -> machine_truth_json` 参数约束
  - `cli_surface monitor.alerts -> alert rendering`（本地 view-model 级）
  - `reactive_trigger -> reactive_runtime -> state_machine_port`（fake state machine 级）
- Not verified seams:
  - `cli_surface -> reactive_runtime` 真实桥接
  - `reactive_runtime -> execution_layer` 端到端
  - wave3 最小跨线程 happy path（真实 adapter + runtime + execution）整体联调

## E. Must-Fix Before/At Next Wave Start
- 修正 wave3 thread/handoff 文档中的分支与 HEAD 锚点漂移。
- 提供至少 1 条真实跨线程命令链路证据（CLI -> adapter -> downstream module）。
- 提供 `reactive_runtime -> execution_layer` 最小端到端证据（含失败路径结构化回执）。

## F. Allowed Carry-Over
- `shadow_monitor` 到 CLI alert 的真实字段映射验证。
- timestamp 单位规范冻结与转换测试。
- bps/pct 单位跨线程契约测试。
- 线程负责人字段补全。

## G. Commands Run
- `git branch --show-current`
- `git rev-parse --short HEAD`
- `git status --short --branch`
- `git log --oneline -n 12`
- `git diff --name-status HEAD~3..HEAD`
- `git diff --stat HEAD~3..HEAD`
- `python -m unittest backend.cli.test_app -v`
- `python -m unittest backend.cli.approval.test_approval_flow -v`
- `python -m unittest backend.cli.views.test_alerts -v`
- `$env:PYTHONPATH='.'; python -m unittest backend.reactive.adapters.test_reactive_runtime -v`

## H. Exit Result
- Gate status: `PASS_WITH_NOTES`
- Next wave may start: `Yes`
- Must-fix items: see section `E`
- Allowed carry-over issues: see section `F`
