# W4 Wave Integration Matrix

- Wave: `W4`
- Checked on: `2026-04-08`
- Branch state observed:
  - branch: `codex/wave4`
  - `HEAD`: `9b85ce5 feat: 实现影子监控与紧急平仓联动`
  - recent commits: `9b85ce5`, `4297287`, `e80a4b8`
  - working tree: clean (`git status --short --branch` => `## codex/wave4...origin/codex/wave4`, `git diff --name-only HEAD` empty)
- Evidence inputs:
  - `docs/acceptance/threads/wave4/emergency_force_close.thread_acceptance.md`
  - `docs/acceptance/threads/wave4/shadow_monitor.thread_acceptance.md`
  - `docs/acceptance/handoffs/wave4/emergency_force_close.thread_to_thread_handoff.md`
  - `docs/acceptance/handoffs/wave4/shadow_monitor.thread_to_thread_handoff.md`
  - `backend/contracts/core/ReactiveInvestmentCompiler.sol`
  - `backend/contracts/interfaces/IReactiveInvestmentCompiler.sol`
  - `backend/monitor/shadow_monitor.py`
  - `backend/monitor/reconciliation_daemon.py`
  - `git diff --name-status HEAD~1..HEAD`
  - `git show --name-status --oneline 9b85ce5`
  - `git log --oneline -n 12`
  - commands run in current workspace:
    - `forge test --root backend/contracts --contracts backend/contracts/core -vv`
    - `python -m py_compile backend/monitor/shadow_monitor.py backend/monitor/reconciliation_daemon.py`

## Integration Relations Checked

| Relation | What was actually checked | Evidence | Alignment observed | Mismatch / gap | Status |
| --- | --- | --- | --- | --- | --- |
| `shadow_monitor.force_close_recommendations -> emergency_force_close` | 对比了 monitor 输出对象与合约 emergency 入口入参。 | `backend/monitor/shadow_monitor.py`, `docs/acceptance/handoffs/wave4/shadow_monitor.thread_to_thread_handoff.md`, `backend/contracts/core/ReactiveInvestmentCompiler.sol`, `docs/acceptance/handoffs/wave4/emergency_force_close.thread_to_thread_handoff.md` | monitor 输出 `action=emergency_force_close`，语义上指向 break-glass；双方都携带 `intent` 语义。 | monitor 只输出 `intent_id(str)` 与业务原因；合约要求 `intentId(bytes32)` + `maxSlippageBps(uint256)` + 授权 caller。`maxSlippageBps` 来源未定义，`str->bytes32` 编码未定义。 | `checked; unresolved payload mismatch` |
| `emergency_force_close -> reactive trigger late callback` | 检查 emergency 写状态与普通触发入口状态检查的代码路径。 | `backend/contracts/core/ReactiveInvestmentCompiler.sol`, `docs/contracts/emergency_force_close.contract.md` | `emergencyForceClose` 先写 `Closed`；`executeReactiveTrigger` 在 `Closed` 时 `revert ClosedIntentCannotExecute`，满足“迟滞回调拒绝”不变量。 | wave4 未见“先 emergencyForceClose 再触发 executeReactiveTrigger”的专项用例证据。 | `code-path checked; test-chain not verified yet` |
| `shadow_monitor upstream ports -> monitor core` | 检查了 daemon 端口协议与 monitor 输入模型校验。 | `backend/monitor/reconciliation_daemon.py`, `backend/monitor/shadow_monitor.py`, `docs/contracts/shadow_monitor.contract.md` | `ActivePositionSourcePort` + `BackupRPCPort` 接口存在；`ActivePosition`/`timezone-aware datetime`/`grace_period_seconds` 约束明确。 | 与真实上游实现（实际数据源/RPC）的联调证据未发现。 | `checked at interface-model level; runtime integration not verified yet` |
| `shadow_monitor alerts -> cli_surface` | 仅核对 handoff 中声明的下游关系。 | `docs/acceptance/handoffs/wave4/shadow_monitor.thread_to_thread_handoff.md`, `docs/acceptance/threads/wave4/shadow_monitor.thread_acceptance.md` | 下游目标已声明为 `cli_surface`。 | 当前 wave4 无 CLI 映射代码或联调证据。 | `not verified yet` |
| `ReactiveInvestmentCompiler concrete ABI -> IReactiveInvestmentCompiler` | 对比了实现合约与共享接口文件。 | `backend/contracts/core/ReactiveInvestmentCompiler.sol`, `backend/contracts/interfaces/IReactiveInvestmentCompiler.sol` | 既有注册/触发/查询接口保持一致。 | 新增 emergency 相关接口（`owner`/`setEmergencyAuthorizedRelayer`/`isEmergencyAuthorizedRelayer`/`emergencyForceClose`）未在 `IReactiveInvestmentCompiler` 声明；依赖接口类型的下游无法直接调用。 | `checked; unresolved interface drift` |

## Cross-Thread Interface Notes

### Field / payload shape
- 已确认不匹配：
  - `intent_id`（monitor: `str`） vs `intentId`（contract: `bytes32`）
  - monitor recommendation 中缺失 contract 必需参数 `maxSlippageBps`
- 已确认未消费字段：
  - `reason_code` / `breached_rule_id` / `estimated_additional_loss_usd` 目前无对应合约入参

### Enum
- monitor 使用字符串枚举：`position_state = ActivePosition | Closed`、`operator = lte | gte`。
- contract 使用 Solidity enum `PositionState { PendingEntry, ActivePosition, Closed }`。
- 跨线程枚举映射规则（字符串到链上 enum/索引）未冻结：`not verified yet`。

### TTL / timestamp unit
- monitor 强约束为 timezone-aware `datetime`（`observed_at`、`checked_at`）和 `grace_period_seconds`。
- emergency 合约接口不接收时间字段。
- 从 monitor 时间语义到下游执行/审批时间字段的统一规则未见证据：`not verified yet`。

### BPS / pct
- emergency 合约使用 `maxSlippageBps`（`0..10000`）。
- monitor recommendation 未输出 bps/pct 字段。
- 从监控告警到 emergency `maxSlippageBps` 的单位换算与默认值策略未定义，属于已确认缺口。

## Evidence Conflicts Found During Wave Check

- wave4 线程文档中多处仍记录 `HEAD=4297287` 与“未提交工作树”状态，但当前实际 `HEAD` 为 `9b85ce5` 且工作树 clean。
- 该锚点漂移会影响“是否已冻结基线”的判断，需要在后续文档回填中统一校正。

## Wave-Level Conclusion

- 本波次已确认的关系主要是“接口/代码路径层检查”，并非端到端联调闭环。
- 存在明确的跨线程 payload/interface 缺口（`intent_id` 类型、`maxSlippageBps` 缺失、共享接口未声明 emergency API）。
- 结论：`W4` 目前不能标记为“integration success”；可确认的是模块实现已落地且风险已定位，跨线程实链路仍需补证。
