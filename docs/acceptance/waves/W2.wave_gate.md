# W2 Wave Gate

- Wave: `W2`
- Gate owner: `Codex`
- Date: `2026-04-01`
- Branch / HEAD observed: `master` @ `1211b50`
- Compared integration diff: working tree vs `1211b50`
- Participating threads reviewed for this gate:
  - `decision_context_builder`
  - `execution_layer`
  - `reactive_runtime`
  - `cli_surface`

## A. Completeness
- All reviewed Wave 2 threads have thread acceptance docs: `yes`
  - `docs/acceptance/threads/wave2/decision_context_builder.thread_acceptance.md`
  - `docs/acceptance/threads/wave2/execution_layer_thread_acceptance.md`
  - `docs/acceptance/threads/wave2/reactive_runtime.thread_acceptance.md`
  - `docs/acceptance/threads/cli_surface.thread_acceptance.md`
- All reviewed Wave 2 threads have handoff docs: `yes`
  - `docs/acceptance/handoffs/wave2/decision_context_builder.thread_to_thread_handoff.md`
  - `docs/acceptance/handoffs/wave2/execution_layer_thread_to_thread_handoff.md`
  - `docs/acceptance/handoffs/wave2/reactive_runtime.thread_to_thread_handoff.md`
  - `docs/acceptance/handoffs/wave2/cli_surface.thread_to_thread_handoff.md`
- All threads are delivered or explicitly documented with remaining gaps: `yes`
  - `decision_context_builder`: `PASS_WITH_NOTES`
  - `execution_layer`: `PASS_WITH_NOTES`
  - `reactive_runtime`: `PASS_WITH_NOTES`
  - `cli_surface`: `PASS_WITH_NOTES`
- Wave packet canonicality is fully stable: `no`
  - `docs/acceptance/05_prefilled_wave_packets/W2_验收包.md` names a different W2 module set than the current reviewed wave2 thread/handoff packet.
  - Some reviewed wave2 acceptance artifacts are present in the working tree but still untracked.

## B. Dependency Alignment
- Upstream/downstream interface freeze is fully verified: `no`
- Sample payload alignment is fully verified: `no`
- Exception-model alignment is fully verified: `partially`
- Naming/path consistency is fully verified: `no`

What is factually supported:
- `execution_layer -> export_outputs` has fresh test-level compatibility evidence in the current workspace.
  - `python -m unittest -v backend/execution/runtime/test_execution_layer.py` -> `4 tests, OK`
  - This test proves `ExecutionRecord.model_dump(mode="json")` can be consumed by `export_outputs(...)`.
- `reactive_runtime -> investment_state_machine_contract` is aligned at the Python port / Solidity interface semantics level.
  - State names align: `PendingEntry | ActivePosition | Closed`.
  - Register-side mapping aligns: `entry_amount_out_minimum -> entry_min_out`, `exit_min_out_floor -> exit_min_out_floor`.
- Integration mismatches are recorded in:
  - `docs/acceptance/waves/W2.wave_integration_matrix.md`
  - `docs/acceptance/waves/W2.wave_risk_register.md`

What remains unverified or mismatched:
- `provider_architecture -> decision_context_builder` is only aligned at the provider envelope level; concrete provider operations and payload fields are not aligned with the normalized fetcher contract.
- `decision_context_builder` has no evidenced non-test downstream consumer in the current wave code.
- `execution_compiler -> reactive_runtime` is only schema-compared; no real handoff path was executed.
- `reactive_runtime -> execution_layer` has no bridge and currently uses different trigger contracts.
- `cli_surface` approval mapping expects camelCase register-payload keys, while current compiler/reactive models are snake_case typed models.

## C. Wave Minimum Integration
- Wave-level minimum happy path verified end-to-end: `no`
- Wave-level minimum happy path explicitly marked unverified: `yes`
- Single-thread/module tests pass in current workspace: `yes`
- Combined cross-thread flow remains unverified: `yes`
- Concrete cross-thread mismatch exists in an intended active seam: `yes`

Current branch-diff fact:
- `git diff --stat HEAD` -> no tracked-file diff
- `git diff --name-only HEAD` -> no tracked-file diff
- The current delta from `HEAD` is untracked content, including:
  - `docs/acceptance/threads/cli_surface.thread_acceptance.md`
  - `docs/acceptance/handoffs/wave2/cli_surface.thread_to_thread_handoff.md`
  - `docs/acceptance/threads/wave2/reactive_runtime.thread_acceptance.md`
  - `docs/acceptance/handoffs/wave2/reactive_runtime.thread_to_thread_handoff.md`
  - `docs/acceptance/waves/W2.wave_integration_matrix.md`
  - `docs/acceptance/waves/W2.wave_risk_register.md`
  - `backend/cli/__pycache__/`
- Because the wave packet is partly untracked, the integration branch does not yet provide a stable committed gate packet.

## D. Invariants Check
- Execution truth remains JSON: `yes`
  - No reviewed wave2 code contradicts `machine truth JSON` as the execution truth.
- Audit/report boundary remains separated: `yes`
  - `cli_surface` only freezes export rails and display surfaces; it does not redefine execution truth.
- Execution compiler remains registration-time only: `yes at code-review level`, `not verified end-to-end`
- Reactive remains limited to trigger/state progression: `yes at code-review and unit-test level`
- Execution layer still requires runtime gate before chain call: `yes`
  - Fresh test evidence: `test_runtime_checks_must_pass_before_any_chain_call`
- Shadow Monitor remains independent: `not verified yet`

## E. Evidence Reviewed
- Acceptance docs reviewed:
  - `docs/acceptance/threads/wave2/decision_context_builder.thread_acceptance.md`
  - `docs/acceptance/threads/wave2/execution_layer_thread_acceptance.md`
  - `docs/acceptance/threads/wave2/reactive_runtime.thread_acceptance.md`
  - `docs/acceptance/threads/cli_surface.thread_acceptance.md`
- Handoff docs reviewed:
  - `docs/acceptance/handoffs/wave2/decision_context_builder.thread_to_thread_handoff.md`
  - `docs/acceptance/handoffs/wave2/execution_layer_thread_to_thread_handoff.md`
  - `docs/acceptance/handoffs/wave2/reactive_runtime.thread_to_thread_handoff.md`
  - `docs/acceptance/handoffs/wave2/cli_surface.thread_to_thread_handoff.md`
- Wave docs reviewed:
  - `docs/acceptance/waves/W2.wave_integration_matrix.md`
  - `docs/acceptance/waves/W2.wave_risk_register.md`
- Repository evidence reviewed:
  - `git status --short --branch`
  - `git log --oneline --decorate -12`
  - `git diff --stat HEAD`
  - `git diff --name-only HEAD`
  - `git show --stat --summary --name-only HEAD`
  - targeted code inspection for provider, context builder, execution, reactive, CLI, export, and contract interfaces
- Commands re-run in current workspace:
  - `$env:PYTHONPATH='D:\reactive-crypto-agentic-DeFi-system'; pytest backend/data/context_builder/test_context_builder.py backend/data/fetchers/test_aggregated_fetchers.py -q` -> `11 passed, 2 warnings`
  - `python -m unittest -v backend/execution/runtime/test_execution_layer.py` -> `4 tests, OK`
  - `python -m unittest backend.reactive.adapters.test_reactive_runtime -v` -> `3 tests, OK`
  - `$env:PYTHONDONTWRITEBYTECODE='1'; pytest backend/cli/tests/test_cli_surface.py -q -p no:cacheprovider` -> `7 passed`
  - Python import check for runtime CLI deps -> `typer=False`, `rich=False`

## F. Evidence Conflicts
- Multiple thread docs still claim the repository had no `HEAD` commit or no commit history.
  - Current repo fact is different: `HEAD=1211b50`.
- Thread docs for `reactive_runtime` and `cli_surface` exist, but remain untracked in the current working tree.
- The prefilled W2 packet and the actual reviewed W2 thread set do not describe the same module scope.

## G. Gate Conclusion
- Conclusion: `FAIL`
- Gate status: `FAIL`
- May the next wave start: `no`

Must-fix before the next wave starts:
- Freeze and verify the `reactive_runtime -> execution_layer` bridge contract.
  - At minimum, define the checked mapping from `ReactiveTrigger(intent_id, kind, observed_out, runtime_exit_min_out)` to `RuntimeTrigger(trigger_id, trade_intent_id, trigger_source, triggered_at, onchain_checks_passed, metadata)`.
- Resolve the `cli_surface` approval payload contract mismatch.
  - Either consume typed snake_case execution-plan objects directly, or freeze and test an explicit adapter DTO that provides the currently expected camelCase fields.
- Reconcile the authoritative W2 module packet.
  - `docs/acceptance/05_prefilled_wave_packets/W2_验收包.md` must not disagree with the actual wave2 acceptance/handoff set used for the gate.
- Commit or otherwise stabilize the current wave2 acceptance packet.
  - Untracked gate evidence is not a stable audit artifact.
- Refresh stale thread-level git evidence that still says `HEAD` / commits are unavailable.

Allowed carry-over issues after the must-fix items above are done:
- Real chain-backed adapter implementation for `reactive_runtime -> investment_state_machine_contract` may move to the first wave that owns on-chain integration, as long as the seam stays explicitly marked `not verified yet`.
- Live provider-backed `decision_context_builder` integration may carry only if downstream work still treats the module as normalized-contract-only and does not claim real provider interoperability.
- `execution_layer -> export_outputs` type-freeze debt may carry if downstream consumers continue to treat the seam as test-level verified rather than production-hardened.
- CLI runtime dependency installation (`Typer`, `Rich`) may carry only if the next wave does not claim end-to-end CLI execution and the gate remains explicit that the CLI surface is still adapter/wiring incomplete.
