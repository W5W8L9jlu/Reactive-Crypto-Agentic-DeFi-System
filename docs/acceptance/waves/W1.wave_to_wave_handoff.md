# W1 to W2 Wave Handoff

- Current Wave: `W1`
- Next Wave: `W2`
- Date: `2026-03-31`
- Gate conclusion observed in current workspace: `FAIL`
- Workspace note:
  - `docs/acceptance/waves/W1.wave_gate.md` and `docs/acceptance/waves/W1.wave_exit_report.md` exist in the workspace but are currently untracked by git.
  - This handoff therefore freezes only what is supported by current code plus the current wave evidence packet, not what is merely implied by desired process state.

## 1. Frozen capabilities from this wave

- `strategy_boundary_service` is frozen at the module boundary level:
  - service methods: `register_template(...)`, `get_template(...)`, `get_latest_version(...)`, `evaluate(...)`
  - shared models: `StrategyTemplate`, `StrategyIntent`, `TradeIntent`, `BoundaryDecisionResult`
  - shared enums: `BoundaryDecision`, `RuleDecision`
  - unit conventions evidenced in code:
    - `position_usd` is `Decimal`
    - `ttl_seconds` is positive integer seconds
    - `*_bps` fields are non-negative integers
    - `execution_mode` is `"conditional"`
- `validation_engine` is frozen at the module boundary level:
  - entry points: `validate_inputs(...)`, `validate_inputs_or_raise(...)`
  - models: `ExecutionHardConstraints`, `ExecutionPlan`, `ValidationIssue`, `ValidationResult`
  - the validation module directly imports `StrategyTemplate`, `StrategyIntent`, and `TradeIntent` from `backend.strategy.models`, so this seam is schema-aligned in current code rather than duplicated by copy
- `provider_architecture` is frozen only at the provider-side contract level:
  - request/response types: `ProviderRequest(operation, params)`, `ProviderResponse(provider, operation, payload, metadata)`
  - policy objects: `TimeoutPolicy`, `RetryPolicy`
  - provider entry points evidenced in code: `RPCProvider.fetch(...)`, `GraphProvider.fetch(...)`, `EtherscanProvider.fetch(...)`, `EtherscanProvider.fetch_or_fallback(...)`
- `export_outputs` is frozen only at the export envelope level:
  - typed wrappers: `DecisionArtifact`, `ExecutionRecord`
  - output object: `ExportOutputs(machine_truth_json, audit_markdown, investment_memo)`
  - `export_outputs(...)` always emits the three-output bundle above when given non-empty inputs
  - `machine_truth_json` is the execution truth artifact; `audit_markdown` and `investment_memo` are derived outputs, not replacement truths

## 2. Safe truths for next wave to rely on

- The only Wave 1 seam that is factually checked across modules is `strategy_boundary_service -> validation_engine`, and that check is schema-level, not end-to-end runtime flow.
- Next-wave work may rely on the shared strategy field names currently present in code:
  - `template_id`
  - `template_version`
  - `strategy_intent_id`
  - `pair`
  - `dex`
  - `position_usd`
  - `max_slippage_bps`
  - `stop_loss_bps`
  - `take_profit_bps`
  - `ttl_seconds`
- Validation currently fails explicitly instead of silently filling gaps:
  - unknown/undefined validation spec for empty `allowed_pairs` or `allowed_dexes` raises `MissingValidationSpecError`
  - parse/domain failures return `ValidationResult(is_valid=False, ...)` in `validate_inputs(...)`
- Export currently fails explicitly instead of silently filling gaps:
  - both `decision_artifact` and `execution_record` empty raises `ExportDomainError`
  - non-JSON-serializable machine truth raises `ExportDomainError`
- Provider code already exposes a reusable transport contract and centralized retry/timeout policy, but no Wave 1 downstream consumer was evidenced.
- Contract code already exposes an interface surface in `IReactiveInvestmentCompiler.sol`, but Wave 1 did not freeze any checked upstream mapping into that surface.
- Core invariants still hold in the current evidence set:
  - machine truth remains structured JSON
  - audit/report outputs remain separated from execution truth
  - no evidence in this wave shows AI generating final calldata, holding funds, or bypassing conditional execution boundaries

## 3. Things the next wave must NOT assume

- Do not assume Wave 1 passed its gate. It did not.
- Do not assume a minimum happy path was verified end-to-end. No such verification was evidenced.
- Do not assume `strategy_boundary_service` or `validation_engine` already define the mapping into contract `InvestmentIntent`.
  - No checked mapping exists yet for:
    - `position_usd -> plannedEntrySize`
    - `*_bps -> entryMinOut / exitMinOutFloor / runtimeExitMinOut`
    - `ttl_seconds -> contract/runtime enforcement ownership`
- Do not assume `export_outputs` freezes the internal field schema of `DecisionArtifact` or `ExecutionRecord`.
  - In current code they are `RootModel[dict[str, Any]]`, so only the outer envelope is frozen.
- Do not assume `provider_architecture` payload shapes are consumer-proven.
  - Wave 1 proved provider-side interfaces, not downstream consumption semantics.
- Do not assume `investment_state_machine_contract` evidence is canonical.
  - Duplicate acceptance/handoff files still exist and differ materially.
- Do not assume Solidity compile/test evidence exists for the contract in this wave. It does not.
- Do not assume thread-level git evidence is reliable as written.
  - Multiple thread artifacts still contain git-state claims that conflict with the current repository state.

## 4. Remaining issues

### Blocker

- Freeze and document the checked mapping from strategy/validation artifacts into contract `InvestmentIntent` semantics before Wave 2 execution/reactive work depends on it.
- Canonicalize `investment_state_machine_contract` evidence by choosing one authoritative acceptance doc and one authoritative handoff doc, then retiring the duplicate variants.
- Refresh thread-level git evidence so wave audit traces match the actual repository state.
- Either verify a Wave 1 minimum happy path with a real command path, or keep downstream dependency claims blocked with explicit sign-off.

### Known gap

- `provider_architecture` still has no evidenced Wave 1 downstream consumer.
- `export_outputs` still has no evidenced Wave 1 upstream producer integration.
- `export_outputs` still carries field-drift risk because `DecisionArtifact` and `ExecutionRecord` are generic dict wrappers.
- `investment_state_machine_contract` still lacks compile/test evidence in the current environment.
- Current workspace/process evidence is inconsistent:
  - some docs describe a clean repo or missing git history
  - current workspace shows a git repo on `master`
  - current workspace also shows untracked wave-level docs

### Acceptable carry-over

- Provider-to-consumer integration verification may move with the first real downstream consumer thread, but it must not be treated as already verified.
- Solidity toolchain validation may move with the first wave that owns contract compilation/testing, but it must not be treated as already done.
- Investment Memo template debt may carry over only if next-wave work does not assume a stricter memo schema than the current export envelope provides.
- The existing schema-only seam between strategy and validation may carry into the next wave, but only as a shared-model dependency, not as proof of full pipeline readiness.

## 5. Suggested next-wave start order

1. Stop treating `W1` as ready for open-ended downstream dependency; first close the wave blockers above.
2. Freeze the authoritative upstream-to-contract mapping table:
   - strategy/validation fields
   - unit conversions
   - TTL ownership
   - min-out semantics
3. Canonicalize the contract thread evidence and repair git-state evidence so there is one auditable source set.
4. Verify one minimum path or explicitly document that Wave 2 is starting without end-to-end dependency rights beyond the frozen seams listed above.
5. Only after steps 1-4, let the first Wave 2 consumer integrate in this order:
   - `strategy_boundary_service -> validation_engine`
   - compiler/adapter mapping into contract intent semantics
   - contract/runtime consumer
   - provider consumer integration
   - export producer integration
