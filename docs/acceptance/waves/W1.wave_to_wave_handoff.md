# W1 to W2 Wave Handoff

- Current Wave: `W1`
- Next Wave: `W2`
- Date: `2026-04-01`
- Gate conclusion observed in current workspace: `PASS`
- Owner: `Codex`
- Branch / HEAD observed: `w1-gate-fail-fix` @ `c5afba2`
- Workspace note:
  - The working tree currently contains unrelated dirty files outside this handoff scope.
  - This document freezes only what is supported by the current W1 gate packet plus current code on the integration branch.
  - Where the older `2026-03-31` integration matrix or thread handoffs still describe pre-compiler gaps, the later `2026-04-01` wave gate / exit report and current code are the freeze point.

## 1. Frozen capabilities from this wave

- `strategy_boundary_service` is frozen at the service and shared-model boundary:
  - service methods: `register_template(...)`, `get_template(...)`, `get_latest_version(...)`, `evaluate(...)`
  - shared models: `StrategyTemplate`, `StrategyIntent`, `TradeIntent`, `BoundaryDecisionResult`
  - shared enums: `BoundaryDecision`, `RuleDecision`
  - shared unit conventions still evidenced in code:
    - `position_usd` is `Decimal`
    - `ttl_seconds` is positive integer seconds
    - `*_bps` fields are non-negative integers
    - `execution_mode` remains `"conditional"`
- `execution_compiler` is now part of the frozen W1 surface:
  - entry points: `compile_execution_plan(...)`, `freeze_contract_call_inputs(...)`
  - explicit inputs: `CompilationContext`, `RegistrationContext`, `ChainStateSnapshot`, `CompilerConfig`
  - frozen payload types: `ExecutionPlan`, `RegisterPayload`, `ContractRegisterCallInputs`, `ContractInvestmentIntent`
  - compiler-owned derivations now frozen in code:
    - `position_usd` + `input_token_usd_price` + token decimals -> `plannedEntrySize`
    - `plannedEntrySize` + `input_output_price` + tightened `max_slippage_bps` -> `entryAmountOutMinimum`
    - `entryAmountOutMinimum` + `stop_loss_bps` -> `exitMinOutFloor`
    - tightened `ttl_seconds` -> `entryValidUntil`
- `validation_engine` is frozen at the validation and contract-binding seam:
  - entry points: `validate_inputs(...)`, `validate_inputs_or_raise(...)`
  - stable models: `ValidationResult`, `ValidationIssue`, `ExecutionPlan`, `ExecutionHardConstraints`, `ContractBinding`
  - on success it now validates real compiler output and freezes bindings for:
    - template / intent identity fields
    - `execution_plan.hard_constraints.*`
    - `register_call.intentId`
    - `investment_intent.owner`
    - `investment_intent.inputToken`
    - `investment_intent.outputToken`
    - `investment_intent.plannedEntrySize`
    - `investment_intent.entryMinOut`
    - `investment_intent.exitMinOutFloor`
- `provider_architecture` is frozen at the provider contract and fetcher-consumer seam:
  - provider-side request / response contract: `ProviderRequest(operation, params)`, `ProviderResponse(provider, operation, payload, metadata)`
  - provider entry points: `RPCProvider.fetch(...)`, `GraphProvider.fetch(...)`, `EtherscanProvider.fetch(...)`, `EtherscanProvider.fetch_or_fallback(...)`
  - evidenced downstream consumers: `AggregatedMarketFetcher`, `AggregatedLiquidityFetcher`, `AggregatedOnchainFetcher`, `AggregatedRiskFetcher`, `AggregatedPositionFetcher`, `AggregatedExecutionFetcher`
- `export_outputs` is frozen at the three-output boundary:
  - entry point: `export_outputs(...)`
  - stable wrappers: `DecisionArtifact`, `ExecutionRecord`, `MachineTruth`, `ExportOutputs`
  - stable output bundle: `machine_truth_json`, `audit_markdown`, `investment_memo`
  - `machine_truth_json` remains the execution truth artifact; `audit_markdown` and `investment_memo` remain derived outputs only
- `investment_state_machine_contract` is frozen at the compile/test-backed interface level:
  - contract methods: `registerInvestmentIntent`, `executeReactiveTrigger`, `getPositionState`, `getActualPositionSize`, `getInvestmentIntent`
  - contract object: `InvestmentIntent(owner, inputToken, outputToken, plannedEntrySize, entryMinOut, exitMinOutFloor)`
  - state enum: `PendingEntry`, `ActivePosition`, `Closed`
  - canonical errors: `IntentAlreadyRegistered`, `IntentNotRegistered`, `ClosedIntentCannotExecute`, `EntryConstraintViolation`, `ExitConstraintViolation`, `RuntimeExitMinOutTooLow`, `ActualPositionSizeNotRecorded`
- Wave-level frozen happy path is now real:
  - Python side: `strategy_boundary_service -> execution_compiler -> validation_engine -> export_outputs`
  - Solidity side: `registerInvestmentIntent -> executeReactiveTrigger(entry) -> executeReactiveTrigger(exit)`

## 2. Safe truths for next wave to rely on

- Wave 1 is handoff-eligible on current evidence. The W1 gate is `PASS` on `2026-04-01`.
- The most important frozen handoff seam is now `ExecutionPlan.register_payload` plus `freeze_contract_call_inputs(...)`, not the earlier hint-only docs.
- Next wave may safely rely on this checked mapping chain:
  - `registration_context.intent_id` -> `register_payload.intentId` -> `register_call.intentId`
  - `registration_context.owner` -> `register_payload.owner` -> `investment_intent.owner`
  - `registration_context.input_token` -> `register_payload.inputToken` -> `investment_intent.inputToken`
  - `registration_context.output_token` -> `register_payload.outputToken` -> `investment_intent.outputToken`
  - `trade_intent.position_usd` + `chain_state.input_token_usd_price` + `chain_state.input_token_decimals` -> `register_payload.plannedEntrySize` -> `investment_intent.plannedEntrySize`
  - `trade_intent.max_slippage_bps` -> tightened `execution_plan.hard_constraints.max_slippage_bps`
  - `register_payload.plannedEntrySize` + `chain_state.input_output_price` + tightened `max_slippage_bps` -> `register_payload.entryAmountOutMinimum` -> `investment_intent.entryMinOut`
  - `trade_intent.stop_loss_bps` -> `execution_plan.hard_constraints.stop_loss_bps` -> `register_payload.stopLossSlippageBps`
  - `register_payload.entryAmountOutMinimum` + `stop_loss_bps` -> `register_payload.exitMinOutFloor` -> `investment_intent.exitMinOutFloor`
  - `trade_intent.take_profit_bps` -> `execution_plan.hard_constraints.take_profit_bps` -> `register_payload.takeProfitSlippageBps`
  - `trade_intent.ttl_seconds` -> tightened `execution_plan.hard_constraints.ttl_seconds` -> `register_payload.entryValidUntil`
- `registration_context` and `chain_state.input_token_usd_price` are now explicit upstream requirements.
  - Downstream work may depend on them being required inputs.
  - Downstream work must not move them back into implicit inference.
- `validation_engine` fails explicitly instead of masking gaps:
  - undefined empty allowlist behavior still raises `MissingValidationSpecError`
  - parse / domain failures still return `ValidationResult(is_valid=False, ...)` in `validate_inputs(...)`
- Provider-backed fetchers are now real consumers of the provider contract:
  - payloads are parsed into typed downstream models
  - missing required payload fields raise `ProviderDomainError`
  - fallback can be exercised when a fallback provider exists
- `export_outputs(...)` preserves machine truth primacy:
  - empty decision artifact plus empty execution record is still an explicit error
  - non-JSON-serializable machine truth is still an explicit error
- The contract state machine is safe to target with the frozen payload:
  - registration starts at `PendingEntry`
  - successful entry advances to `ActivePosition` and records `actualPositionSize`
  - successful exit advances to `Closed`
- Canonical verification commands for the frozen W1 seam are:
  - `pytest backend/strategy/tests/test_strategy_boundary_service.py -q`
  - `$env:PYTHONPATH='.'; pytest backend/data/fetchers/test_aggregated_fetchers.py -q`
  - `python -m unittest backend.execution.compiler.test_execution_compiler -v`
  - `python -m unittest backend.validation.test_validation_engine -v`
  - `python -m unittest backend.export.test_export_outputs -v`
  - `D:\Foundry\bin\forge.exe build --root . --contracts backend/contracts`
  - `$env:FOUNDRY_CACHE_PATH='D:/reactive-crypto-agentic-DeFi-system/.foundry-cache'; D:\Foundry\bin\forge.exe test --root . --contracts backend/contracts --match-path 'backend/contracts/test/ReactiveInvestmentCompiler.t.sol' -vv`

## 3. Things the next wave must NOT assume

- Do not assume the earlier `2026-03-31` W1 integration matrix is the final word where the later `2026-04-01` gate / exit report and current code closed gaps. The compiler freeze is real now.
- Do not assume `BoundaryDecisionResult.contract_binding_hints` or `validation_engine.contract_bindings` are final calldata or the canonical register payload.
  - The frozen execution artifact is the compiler output: `ExecutionPlan.register_payload` and `freeze_contract_call_inputs(...)`.
- Do not assume `intent_id`, `owner`, `input_token`, `output_token`, or `input_token_usd_price` can be silently inferred inside Wave 1 modules.
  - They are explicit inputs now and must stay explicit.
- Do not assume there is a single one-command full-stack Python-to-Solidity harness.
  - W1 froze the payload mapping and checked command sequence.
  - It did not add one monolithic end-to-end runner.
- Do not assume `DecisionArtifact` and `ExecutionRecord` have a fully typed inner field schema.
  - They are still generic dict wrappers.
  - The frozen guarantee is the outer export envelope plus the canonical happy-path payloads preserved in machine truth.
- Do not assume provider architecture owns business decisions or broader context-builder semantics.
  - W1 froze provider request / response and typed fetcher consumption.
  - It did not freeze every future consumer shape.
- Do not assume post-close settlement / accounting semantics exist in the contract.
  - Current contract coverage stops at register, entry, exit, and state / position queries.
- Do not assume environment rough edges are already solved.
  - `provider_architecture` tests still rely on `PYTHONPATH='.'`
  - `.pytest_cache` warnings remain non-blocking noise

## 4. Remaining issues

### Blocker

- None for depending on the frozen W1 interfaces exactly as documented above.

### Known gap

- `DecisionArtifact` and `ExecutionRecord` still carry field-drift risk because their inner payloads remain generic dicts.
- W1 still does not include a single automated harness that feeds Python compiler output directly into the Solidity test path.
- Some older thread handoff wording for `strategy_boundary_service` / `validation_engine` still reflects the pre-re-evaluation state where compiler work was pending.
- Provider-backed fetcher coverage is real, but broader multi-module context-builder verification still belongs to later consumer work.
- Contract post-exit settlement / accounting semantics remain intentionally out of scope.

### Acceptable carry-over

- Investment Memo formal template remains unspecified as long as W2 depends only on:
  - the three-output envelope
  - machine truth as the execution source of truth
- Non-blocking environment noise may carry over:
  - `PYTHONPATH='.'` requirement in the current provider pytest shape
  - `.pytest_cache` permission warnings
- Unrelated dirty files outside the W1 re-evaluation scope may remain in the workspace and should not be treated as part of this freeze.

## 5. Suggested next-wave start order

1. Start from the frozen compiler seam: `CompilationContext` -> `ExecutionPlan.register_payload` -> `freeze_contract_call_inputs(...)`.
2. Build W2 consumers against the contract's actual frozen target fields: `intentId`, `owner`, `inputToken`, `outputToken`, `plannedEntrySize`, `entryMinOut`, `exitMinOutFloor`.
3. Keep `registration_context` and registration-time chain inputs explicit in every new integration; do not reintroduce implicit inference.
4. If W2 needs richer export / CLI / report consumers, freeze a stricter `DecisionArtifact` / `ExecutionRecord` inner schema before multiplying producers and consumers.
5. Treat settlement / accounting semantics, memo-template expansion, and broader context-builder integration as new W2+ scope, not as promises already delivered by W1.
