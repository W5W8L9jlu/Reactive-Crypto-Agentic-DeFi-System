# W1 Wave Risk Register

- Wave: `W1`
- Checked on: `2026-03-31`
- Branch state observed: `master`, working tree clean, single reachable commit `ed817b3`

## High Risks

| Risk | Impact | Current status | Mitigation action | Owner | Due |
| --- | --- | --- | --- | --- | --- |
| No verified compilation/mapping path from strategy or validation artifacts into contract `InvestmentIntent` | Wave 2 execution/reactive threads may invent their own mapping for `position_usd`, `*_bps`, `ttl_seconds`, `entryMinOut`, `exitMinOutFloor`, causing semantic drift or unsafe runtime behavior | `open` | Freeze a checked mapping table before Wave 2 integration: which upstream field populates `plannedEntrySize`, how `*_bps` become min-out constraints, and where TTL is enforced | `not assigned yet` | `not verified yet` |
| `export_outputs` uses generic dict envelopes for `DecisionArtifact` / `ExecutionRecord` | Producers and consumers can drift on field names or nesting without type-level detection, and Wave 2 CLI/adapter work may serialize inconsistent machine truth | `open` | Freeze the canonical `DecisionArtifact` / `ExecutionRecord` field contract before adding more downstream consumers | `not assigned yet` | `not verified yet` |
| Thread-level evidence about git state is inconsistent with the actual branch | Acceptance/handoff docs currently contain false negative evidence (`not a git repository`, `no commits`), so audit traceability is unreliable at wave level | `open` | Refresh thread evidence that mentions git state and distinguish between `repo exists` and `HEAD^ unavailable because this is the initial commit` | `not assigned yet` | `not verified yet` |

## Medium Risks

| Risk | Impact | Current status | Mitigation action |
| --- | --- | --- | --- |
| `provider_architecture` has no verified wave1 downstream consumer | Provider request/response shape may still drift when `decision_context_builder` or other data consumers start integrating | `open` | When the first downstream consumer is implemented, add a real provider-to-consumer integration check instead of relying only on provider-side smoke evidence |
| `investment_state_machine_contract` lacks compile/test evidence in current wave packet | Contract interface may look stable in docs but still fail once Solidity toolchain validation runs | `open` | Run contract compile/test in the consumer wave and attach evidence to replace the current doc-only interface check |
| Duplicate `investment_state_machine_contract` acceptance/handoff filenames create canonical-source ambiguity | Reviewers may cite different artifacts for the same module and miss changes between the duplicate files | `open` | Pick one canonical filename pattern and retire the duplicate variant from future wave evidence |

## Accepted Debt

| Item | Why acceptable for now | Planned wave |
| --- | --- | --- |
| `strategy_boundary_service -> validation_engine` has schema-level alignment but no dedicated end-to-end integration test | The downstream validation module imports the exact upstream strategy models, so this seam has stronger evidence than the other W1 relations even without a separate E2E command | `Wave 2 or later` |
| Investment Memo template remains undefined in knowledge docs | Export surface is present and tested at module level, but the memo content contract is still presentation-layer debt rather than a blocker for core Wave 1 schema alignment | `Wave 2 or later` |
