# W2 Wave Exit Report

- Wave: `W2`
- Date: `2026-04-07`
- Branch / HEAD observed: `w1-gate-fail-fix` @ `c5afba2`
- Working tree state: `dirty`
- Exit conclusion: `FAIL`

## A. What Was Factually Verified In The Current Workspace
- `decision_context_builder`
  - `$env:PYTHONPATH='.'; pytest backend/data/fetchers/test_aggregated_fetchers.py backend/data/context_builder/test_context_builder.py -q`
  - Result: `14 passed, 2 warnings`
  - Verified scope: provider-backed builder happy path, explicit fallback path, explicit error path, `DecisionContext` completeness.
- `execution_compiler`
  - `python -m unittest backend.execution.compiler.test_execution_compiler -v`
  - Result: `2 tests OK`
  - Verified scope: compiler freezes register payload and freezes contract-facing call input shape.
- `validation_engine`
  - `python -m unittest backend.validation.test_validation_engine -v`
  - Result: `6 tests OK`
  - Verified scope: validation consumes compiler-shaped payloads and preserves contract-facing bindings.
- `export_outputs`
  - `python -m unittest backend.export.test_export_outputs -v`
  - Result: `4 tests OK`
  - Verified scope: compiler/validation artifacts can be exported without polluting machine truth.
- `approval_flow`
  - `python -m unittest backend.cli.approval.test_approval_flow -v`
  - Result: `5 tests OK`
  - Verified scope: battle-card rendering, `raw=True`, approve/reject branching, expired approval block.
- `pre_registration_check`
  - `from backend.validation.pre_registration_check import run_pre_registration_check`
  - Result: import smoke `passed`
  - Verified scope: current repo import chain is no longer blocked.
  - inline Python command: `run_pre_registration_check(...)` on explicit negative scenarios
  - Result:
    - `expired:False:ExpiredIntentError`
    - `balance:False:InsufficientBalanceError`
    - `allowance:False:InsufficientAllowanceError`
    - `gas:False:GasTooHighError`
    - `slippage:False:SlippageExceededError`
    - `profit:False:UnprofitableRegistrationError`
    - `health:False:HealthFactorTooLowError`
  - Verified scope: precheck abort-path behavior is reproducible in the current workspace.
- `pre_registration_check -> execution_compiler`
  - inline Python command: run `run_pre_registration_check_or_raise(...)` first, then `compile_execution_plan(...)`
  - Result: `precheck:True:3540`, `plan:ti-001:1710003540:27`
  - Verified scope: one sequential register-time happy path now reaches `ExecutionPlan` in the current workspace.

## B. What Remains Unverified
- `decision_context_builder -> pre_registration_check` consumption seam.
- direct stable consumption of `PreRegistrationCheckResult` by compiler; the current happy path still uses explicit sequential invocation plus shared inputs, not a frozen adapter.
- Real provider payload translation into `decision_context_builder`.
- CLI route / adapter end-to-end wiring for `approval_flow`.

## C. Document Conflicts Found During This Exit Review
- `docs/acceptance/threads/wave2/pre_registration_check.thread_acceptance.md`
  - stale on current evidence: it still says package import is blocked, but current import smoke passes.
- `docs/acceptance/threads/wave2/pre_registration_check.test_evidence.md`
  - stale on current evidence: it still reports the old import failure.
- `docs/acceptance/threads/wave2/execution_compiler.thread_acceptance.md`
  - stale on current evidence: it still says compiler happy path is not verified, but current unit tests pass.
- `docs/acceptance/handoffs/wave2/execution_compiler.thread_to_thread_handoff.md`
  - stale on current evidence: it still describes the module as schema-level only.
- These four files were not refreshed in this pass because the allowed edit scope for this task is limited to wave-level docs only.

## D. Integration Readout
- Verified seams:
  - `strategy models -> execution_compiler`
  - sequential `pre_registration_check -> execution_compiler`
  - `execution_compiler -> validation_engine`
  - `execution_compiler -> export_outputs`
  - `execution_compiler / validation_engine -> approval_flow`
- Not verified seams:
  - `decision_context_builder -> pre_registration_check`
  - frozen adapter / direct contract for `PreRegistrationCheckResult -> CompilationContext`
  - live provider payloads -> `decision_context_builder`
- Known integration mismatches already recorded at wave level:
  - approval TTL display source can diverge from compiler-frozen expiry
  - approval battle-card mapping still has dual implementation surface

## E. Current Integration Branch Diff Snapshot
- `git diff --stat` result:
  - `76 files changed, 2291 insertions(+), 4624 deletions(-)`
- Observed characteristics:
  - worktree is not wave-scoped
  - there are unrelated tracked deletions
  - there are untracked W2 module files
  - gate evidence therefore depends on current workspace state, not a clean frozen commit

## F. Must-Fix Before The Next Wave
- Refresh stale W2 thread docs so handoff consumers are not reading contradicted evidence.
- Cut a clean W2 snapshot before using this wave as the baseline for downstream work.

## G. Allowed Carry-Over
- approval TTL truth-source mismatch.
- duplicate approval battle-card mapper surface.
- Real provider integration for `decision_context_builder`.
- `DecisionContext` downstream consumer verification.
- freshness / staleness policy verification.
- approval CLI route / adapter end-to-end verification.
- stronger type freezing around export envelopes.

## H. Commands Run
- `$env:PYTHONPATH='.'; pytest backend/data/fetchers/test_aggregated_fetchers.py backend/data/context_builder/test_context_builder.py -q`
- `python -m unittest backend.execution.compiler.test_execution_compiler -v`
- `python -m unittest backend.validation.test_validation_engine -v`
- `python -m unittest backend.export.test_export_outputs -v`
- `python -m unittest backend.cli.approval.test_approval_flow -v`
- `@' from backend.validation.pre_registration_check import run_pre_registration_check; print(run_pre_registration_check.__name__) '@ | python -`
- inline Python command: `run_pre_registration_check(...)` abort-path scenarios
- inline Python command: `run_pre_registration_check_or_raise(...)` then `compile_execution_plan(...)`
- `git diff --stat`
- `git log --oneline -12`

## I. Exit Result
- Gate status: `FAIL`
- Next wave may start: `No`
- Must-fix items: see section `F`
- Allowed carry-over issues: see section `G`
