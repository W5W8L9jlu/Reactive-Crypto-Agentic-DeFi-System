# W3 Wave Risk Register

- Wave: `W3`
- Checked on: `2026-04-08`
- Branch state observed:
  - branch: `codex/w3-baseline`
  - `HEAD`: `817c32f`
  - recent commits: `817c32f`, `b920945`, `9177017`
  - working tree: clean

## High Risks

| Risk | Impact | Current status | Mitigation action | Owner | Due |
| --- | --- | --- | --- | --- | --- |
| CLI 命令面仍未绑定真实服务 adapter（默认路径为 `RouteBindingMissingError`） | 一旦不通过测试桩注入，`strategy/decision/approval/execution/export/monitor` 命令会在运行时直接失败，无法形成真实集成链路 | `open` | 冻结并实现 `CLISurfaceServices` 到真实下游模块的 adapter 绑定，并补一条跨线程 CLI 集成测试 | `not assigned yet` | `before any W3 integration success claim` |
| `reactive_runtime` 只在 `FakeStateMachine` 上验证 | 真实状态机合约回执、异常结构、状态同步时序可能与假实现不同，导致回调校验在联调时失效 | `open` | 增加真实状态机适配器集成测试（至少覆盖 `entry/stop_loss/take_profit` 三类触发） | `not assigned yet` | `before execution-layer or on-chain integration sign-off` |
| `cli_surface` 与 `reactive_runtime` 尚无显式 payload adapter | runtime 结果对象（结构化）无法直接落到 CLI 当前字符串返回契约，容易出现“临时拼接字符串”导致字段丢失或语义漂移 | `open` | 定义并冻结 `ReactiveRuntimeResult -> CLI rendered view` 的显式转换层及契约测试 | `not assigned yet` | `before wiring runtime status/ops commands` |
| timestamp 单位跨线程尚未冻结（`datetime` vs epoch seconds） | 触发时间与执行/注册时间的比较可能出现单位或时区偏差，造成 TTL/超时判断错误 | `open` | 明确 wave 级时间字段规范（UTC + epoch seconds or UTC datetime），并补转换/比较测试 | `not assigned yet` | `before first cross-thread time-based decision path` |
| 线程文档中的分支与 HEAD 锚点存在漂移 | 审核者可能基于过期锚点误判“已提交基线”与“已验证范围”，导致错误放行 | `open` | 统一回填 thread_acceptance/delivery_note/handoff 的实际分支与 commit 锚点 | `not assigned yet` | `before wave gate review` |

## Medium Risks

| Risk | Impact | Current status | Mitigation action |
| --- | --- | --- | --- |
| `monitor.alerts` 仅验证了本地 `AlertView` 渲染，未验证上游 `shadow_monitor` 实际字段映射 | 上游真实告警字段一旦变化，CLI 可能出现展示缺字段或排序异常 | `open` | 在 CLI 侧增加与 shadow monitor 输出契约的对接测试，冻结字段映射 |
| Wave3 文档分布不一致（`cli_surface` 在 `wave3/` 子目录，`reactive_runtime` 在根目录） | 波次扫描容易遗漏模块文档，导致集成矩阵信息不完整 | `open` | 统一 Wave3 文档归档规则，或在 wave 文档中固定“模块清单 + 实际路径” |
| 本波次未落地 bps/pct 跨线程链路验证 | 后续接入执行/审批链路时，可能在百分比展示或阈值比较上出现单位歧义 | `open` | 在下一波首次接入 bps/pct 数据时添加单位契约测试（输入、存储、展示全链路） |

## Accepted Debt

| Item | Why acceptable for now | Planned wave |
| --- | --- | --- |
| `cli_surface -> cryptoagents_adapter/execution_layer/reactive_runtime` 真实链路仍为 `not verified yet` | 当前证据只覆盖 CLI 路由层与参数约束，强行宣称端到端集成会违背事实 | `next integration wave` |
| `reactive_runtime -> execution_layer` 回执链路未验证 | 本波次只实现 reactive adapter 最小闭环与 fake state-machine 校验，尚未提供 execution 对接证据 | `next execution-runtime integration wave` |
| bps/pct 跨线程单位对齐未验证 | Wave3 已验证关系中没有 bps/pct 实际传递点，当前只能记录风险，不应脑补结论 | `next wave that carries slippage/threshold payloads` |
