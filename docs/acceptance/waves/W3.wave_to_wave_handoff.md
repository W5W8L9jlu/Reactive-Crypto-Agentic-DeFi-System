# W3 to W4 Wave Handoff

- Current Wave: `W3`
- Next Wave: `W4`
- Date: `2026-04-08`
- Gate conclusion: `PASS_WITH_NOTES`
- Branch / HEAD observed: `codex/w3-baseline` @ `817c32f`
- Working tree at gate review: `dirty (untracked wave docs)`
- Sources of truth used:
  - `docs/acceptance/waves/W3.wave_gate.md`
  - `docs/acceptance/waves/W3.wave_integration_matrix.md`
  - `docs/acceptance/waves/W3.wave_risk_register.md`
  - `docs/acceptance/waves/W3.wave_exit_report.md`
  - `docs/acceptance/handoffs/wave3/cli_surface.thread_to_thread_handoff.md`
  - `docs/acceptance/handoffs/reactive_runtime.thread_to_thread_handoff.md`
  - current code: `backend/cli/app.py`, `backend/cli/errors.py`, `backend/cli/views/alerts.py`, `backend/reactive/adapters/models.py`, `backend/reactive/adapters/runtime.py`

## Frozen capabilities from this wave

- CLI surface command-plane scaffold is frozen at route-contract level (`9177017`):
  - `create_cli_app(services=CLISurfaceServices(...)) -> typer.Typer`
  - command groups: `strategy`, `decision`, `approval`, `execution`, `export`, `monitor`
  - stable service signatures in `CLISurfaceServices`:
    - `strategy_list() -> str`
    - `decision_run(context_id: str) -> str`
    - `approval_show(raw: bool, machine_truth_json: str | None) -> str`
    - `approval_approve(trade_intent_id: str) -> str`
    - `approval_reject(trade_intent_id: str, reason: str) -> str`
    - `execution_status(trade_intent_id: str) -> str`
    - `export_bundle(trade_intent_id: str) -> str`
    - `monitor_alerts(critical_only: bool) -> Sequence[AlertView]`
  - stable CLI error model:
    - `CLISurfaceError`
    - `RouteBindingMissingError`
    - `CLISurfaceInputError`
  - frozen parameter guard:
    - `approval show --raw` must provide `--machine-truth-json`, else `typer.Exit(code=2)`.
- Alert view contract is frozen at local rendering level:
  - `AlertSeverity = warning | critical`
  - `AlertView(code, severity, message, source, escalation_required)`
  - rendering functions: `build_alerts_table(...)`, `render_alerts_snapshot(...)`
  - critical-first ordering and empty-state rendering are test-backed.
- Reactive runtime adapter contract is frozen at module boundary (`817c32f`):
  - entrypoints:
    - `run_reactive_runtime_or_raise(...) -> ReactiveRuntimeResult`
    - `run_reactive_runtime(...) -> ReactiveRuntimeResult`
  - state machine port contract:
    - `get_position_state(intent_id)`
    - `execute_entry_callback(intent, trigger)`
    - `execute_exit_callback(intent, trigger)`
  - frozen trigger/callback/state enums:
    - `ReactiveTriggerType = entry | stop_loss | take_profit`
    - `ReactiveCallbackType = entry_callback | exit_stop_loss_callback | exit_take_profit_callback`
    - `InvestmentPositionState = PendingEntry | ActivePosition | Closed`
  - frozen runtime result surface:
    - success path: `is_executed=True`, `callback_verified=True`, no `abort_reason`
    - abort path: `is_executed=False`, `callback_verified=False`, structured `abort_reason`.

## Safe truths for next wave to rely on

- You can rely on CLI command routing skeleton and handler signatures as stable integration entry points.
- You can rely on explicit missing-binding behavior (`RouteBindingMissingError`) instead of silent fallback.
- You can rely on `approval show --raw` argument rule and error exit semantics (`code=2`).
- You can rely on reactive runtime trigger binding checks:
  - `reactive_trigger.intent_id` must match `registered_intent.intent_id`
  - if `trade_intent_id` exists on intent, trigger must match it.
- You can rely on reactive runtime callback verification semantics:
  - `entry -> entry_callback + state_after=ActivePosition`
  - `stop_loss -> exit_stop_loss_callback + state_after=Closed`
  - `take_profit -> exit_take_profit_callback + state_after=Closed`
- You can rely on the fact that W3 did not freeze any cross-thread adapter between CLI and runtime/execution; this is explicitly unresolved, not implicit.

## Things the next wave must NOT assume

- Do not assume CLI routes are already bound to real downstream services; default behavior is still missing-binding error.
- Do not assume `cli_surface -> reactive_runtime` adapter exists.
- Do not assume `reactive_runtime -> execution_layer` tx receipt/log chain is implemented or verified.
- Do not assume `machine_truth_json` in CLI raw mode is schema-validated against real `export_outputs`.
- Do not assume monitor alert payloads from real `shadow_monitor` already match `AlertView`.
- Do not assume timestamp units are frozen across modules (`datetime` vs epoch seconds is still unresolved).
- Do not assume bps/pct cross-thread unit alignment has been verified in W3.

## Remaining issues

- Blocker (for claiming cross-thread integration success):
  - `cli_surface` real adapter wiring to downstream modules is missing.
  - `reactive_runtime -> execution_layer` end-to-end evidence is missing.
  - wave3 thread/handoff docs still contain branch/HEAD anchor drift that must be corrected for reliable traceability.
- Known gap:
  - `machine_truth_json` raw echo path is not yet tied to exported structured payload contract.
  - monitor upstream field mapping (`shadow_monitor -> AlertView`) is not verified.
  - timestamp and bps/pct cross-thread unit contracts are not frozen.
- Acceptable carry-over:
  - thread owner fields remain `not verified yet`.
  - production packaging/install route for CLI entrypoint remains `not verified yet`.

## Suggested next-wave start order

1. Fix documentation anchors first (thread/handoff branch + HEAD + commit references) to stabilize evidence baseline.
2. Implement one minimal real CLI adapter chain (`CLI command -> adapter -> downstream module`) and add integration evidence.
3. Implement one minimal `reactive_runtime -> execution_layer` end-to-end path with structured failure receipt assertions.
4. Freeze timestamp contract for cross-thread boundaries and add conversion/assertion tests.
5. Freeze monitor payload mapping (`shadow_monitor` output to `AlertView`) and add contract tests.
6. Bind `approval show --raw` to a real exported machine-truth source, then verify payload shape expectations.

## Most important frozen interfaces

- CLI route interface:
  - `create_cli_app(...)`
  - `CLISurfaceServices` handler signatures listed above
  - `CLISurfaceError` / `RouteBindingMissingError` / `CLISurfaceInputError`
- CLI monitor view interface:
  - `AlertSeverity`, `AlertView`, `build_alerts_table(...)`, `render_alerts_snapshot(...)`
- Reactive runtime interface:
  - `run_reactive_runtime_or_raise(...)`
  - `run_reactive_runtime(...)`
  - `InvestmentStateMachinePort`
  - `ReactiveRuntimeResult` + `RuntimeAbortReason`
  - trigger/callback/state enums

## Biggest assumptions the next wave must avoid

- “CLI already integrated with real services.”
- “Reactive runtime already integrated with execution layer.”
- “Raw machine truth display equals validated export schema.”
- “Time-unit and bps/pct-unit contracts are already consistent across modules.”
