# W4 Wave Risk Register

- Wave: `W4`
- Checked on: `2026-04-08`
- Branch state observed:
  - branch: `codex/wave4`
  - `HEAD`: `9b85ce5`
  - recent commits: `9b85ce5`, `4297287`, `e80a4b8`
  - working tree: clean

## High Risks

| Risk | Impact | Current status | Mitigation action | Owner | Due |
| --- | --- | --- | --- | --- | --- |
| `shadow_monitor` 推荐载荷无法直接驱动 `emergencyForceClose`（`intent_id` 类型与 `maxSlippageBps` 缺口） | 告警即使升级为 critical，也无法稳定生成可执行的合约调用参数，联动可能停在“建议”层。 | `open` | 冻结 `ForceCloseRecommendation -> emergencyForceClose` 适配契约：明确 `str -> bytes32` 编码、`maxSlippageBps` 来源与默认策略，并补契约测试。 | `not assigned yet` | `before any auto/manual force-close integration claim` |
| `IReactiveInvestmentCompiler` 未声明 emergency 新接口 | 依赖接口类型的下游调用方无法以统一 ABI 调用 emergency 入口，容易出现编译时/运行时集成断裂。 | `open` | 在接口层补齐 emergency API 声明，或显式定义独立 emergency interface 并更新下游依赖。 | `not assigned yet` | `before downstream contract adapter freeze` |
| 缺少 wave4 跨线程端到端证据（monitor 告警 -> emergency 调用） | 可能误判“已联动”，但实际仅有模块内逻辑与文档声明，生产链路不可用。 | `open` | 增加最小 E2E 联调用例（至少覆盖：grace->critical->生成调用参数->合约返回/事件校验）。 | `not assigned yet` | `before W4 gate PASS consideration` |
| emergency 专项最小验证仍不完整 | 关键安全路径（权限、非 Active 拒绝、force-close 后迟滞回调）若缺专项测试，回归时风险高。 | `open` | 增补 forge 专项测试，覆盖 contract 文档要求的 minimum verification。 | `not assigned yet` | `before release candidate` |
| wave4 线程文档锚点与当前分支状态漂移（仍写 `4297287`） | 审阅者可能依据过期锚点作出错误 gate 判断。 | `open` | 回填 wave4 thread acceptance / delivery / handoff 的真实 HEAD 与分支状态。 | `not assigned yet` | `before wave gate review` |

## Medium Risks

| Risk | Impact | Current status | Mitigation action |
| --- | --- | --- | --- |
| monitor 时间语义（timezone-aware datetime + grace seconds）与下游时间字段规范未冻结 | 未来接入执行/审批线程时，可能出现时间比较和窗口判定偏差。 | `open` | 在 wave 级契约补充时间字段规范（格式、时区、单位）并加转换测试。 |
| `estimated_additional_loss_usd` 仍为临时公式（代码 TODO） | 风险估算口径在跨线程展示/动作优先级中可能漂移。 | `open` | 冻结损失估算公式契约并在 monitor/CLI 文档同步版本。 |
| `shadow_monitor -> cli_surface` 字段映射尚无实证 | 告警展示可能出现字段缺失或语义偏差。 | `open` | 增加实际 payload 到 `AlertView` 的映射测试与样例快照。 |

## Accepted Debt

| Item | Why acceptable for now | Planned wave |
| --- | --- | --- |
| monitor 仅输出 `force_close_recommendation`，未直接执行链上动作 | 符合“只看不摸，除非报警”的模块边界；当前阶段可接受建议式输出。 | `next integration wave` |
| emergency 仍保留 execution adapter / settlement receipt 的 `TODO(domain)` | knowledge/contract 尚未定义该 schema，当前保留 TODO 比脑补实现更安全。 | `next execution-adapter wave` |
| 跨线程 timestamp 与 bps/pct 统一策略尚未冻结 | 当前已识别缺口且未虚假宣称完成，风险可控但需尽快收敛。 | `next wave that wires monitor->execution` |
