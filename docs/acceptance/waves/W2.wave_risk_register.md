# W2 Wave Risk Register

- Wave: `W2`
- Checked on: `2026-04-07`
- Branch state observed:
  - branch: `w1-gate-fail-fix`
  - `HEAD`: `c5afba2 docs: 更新Wave2验收与交接文档`
  - recent commits: `c5afba2`, `1211b50`
  - working tree: dirty; current W2 acceptance conclusions depend on modified/untracked worktree files, not on a clean commit-only snapshot

## High Risks

| Risk | Impact | Current status | Mitigation action | Owner | Due |
| --- | --- | --- | --- | --- | --- |
| `decision_context_builder` has only been proven against normalized mock provider payloads, not against concrete provider implementations | The first live integration can fail immediately on operation naming or payload key mismatch, because fetchers currently expect normalized operations like `market_trend` / `liquidity_depth` while checked providers still expose raw RPC / Graph / Etherscan request semantics | `open` | Freeze and implement an explicit translation layer from concrete provider payloads to the normalized fetcher contract, then add at least one real provider-backed integration test | `not assigned yet` | `before any live provider integration claim` |
| `pre_registration_check -> execution_compiler` is still unverified and the two modules currently own overlapping gas/TTL concepts | Registration gating and compiled register payload can drift on `ttl_buffer_seconds`, `max_gas_price_gwei`, slippage interpretation, and upstream ownership of `expected_profit_usd` / health-factor fields | `open` | Freeze one explicit adapter contract between `PreRegistrationCheckResult` and `CompilationContext`, including authority for TTL buffer, gas cap, and slippage units | `not assigned yet` | `before claiming W2 register-path integration` |
| `approval_flow` shows TTL countdown from `DecisionMeta.ttl_seconds`, not from compiler-frozen `register_payload.entry_valid_until` | Operator UI can show an approval window that is longer than the real register deadline after compiler-side TTL tightening, creating unsafe manual approval decisions | `open` | Make approval countdown derive from compiler-frozen expiry or explicitly prove that `DecisionMeta.ttl_seconds` is always the same effective TTL source | `not assigned yet` | `before using approval UI for real plans` |
| Approval battle-card mapping currently exists in two places: stable typed path in `backend/cli/approval/flow.py` and legacy mapping-style path in `backend/cli/models.py` | Different callers can render different payload assumptions (`typed snake_case` vs `.get('entryValidUntil')` camelCase mapping), producing hidden runtime breakage or inconsistent operator views | `open` | Remove or retire the legacy mapper and freeze `backend/cli/approval/flow.py` as the only supported entrypoint | `not assigned yet` | `before CLI adapter wiring` |

## Medium Risks

| Risk | Impact | Current status | Mitigation action |
| --- | --- | --- | --- |
| `decision_context_builder` still has no verified downstream consumer in this wave | `DecisionContext.generated_at`, `sources`, `aggregated_at`, and freshness semantics can drift before the first consumer arrives | `open` | Add one checked downstream consumer or freeze a narrower handoff DTO before the next consumer wave |
| `execution_compiler -> export_outputs` is currently proven only through a module test and generic dict envelopes | Future producers can change nested fields inside `ExecutionRecord` / `DecisionArtifact` without type-level rejection | `open` | Freeze a typed export-facing record contract or add a dedicated cross-module contract test that fails on unexpected field drift |
| Several W2 thread docs are now stale relative to the current worktree (`execution_compiler` still described as schema-only, `pre_registration_check` import still described as blocked) | Reviewers can sign off using outdated thread-level evidence instead of the actual branch state | `open` | Refresh thread acceptance/handoff docs to match the current worktree evidence before final wave sign-off |

## Accepted Debt

| Item | Why acceptable for now | Planned wave |
| --- | --- | --- |
| `decision_context_builder -> pre_registration_check` remains `not verified yet` | Current wave has no checked adapter and no user request to invent one; documenting the seam honestly is safer than masking the gap with a guessed integration path | `Next wave that consumes DecisionContext or precheck` |
| `pre_registration_check` has no dedicated automated test file yet | Package import now works and the module contract is documented, but no end-to-end consumer exists yet; this is debt, not evidence of integration success | `Next wave that integrates register gating` |
| Approval raw view is only proven to echo a supplied `machine_truth_json` string, not a real `export_outputs(...)` result | The current behavior is still correctly documented as a local interface guarantee; export-to-approval end-to-end wiring remains explicitly unverified | `Next CLI / export integration wave` |
