# W2 Wave Risk Register

- Wave: `W2`
- Checked on: `2026-04-01`
- Branch state observed: `master`, `HEAD=1211b50 feat: 实现Wave2执行层与验收文档回填`, working tree not clean with additional untracked wave2 acceptance artifacts

## High Risks

| Risk | Impact | Current status | Mitigation action | Owner | Due |
| --- | --- | --- | --- | --- | --- |
| `decision_context_builder` currently integrates only with mock-normalized provider payloads, while checked provider implementations expose raw RPC / Graph / Etherscan request semantics and raw payload shapes | The first live data integration can fail immediately on operation naming or payload-field mismatch, or force an undocumented adapter layer at the last minute | `open` | Freeze and implement one explicit translation layer between concrete providers and normalized fetcher payloads, then add at least one real provider-backed integration test before calling the wave integrated | `not assigned yet` | `before first live provider integration` |
| `reactive_runtime` cannot currently drive `execution_layer` because the trigger contracts are different and no bridge exists | Reactive callbacks may stop at the state-machine port and never reach execution, or downstream teams may invent incompatible trigger adapters with missing timestamps / gate fields | `open` | Freeze a concrete bridge contract for `ReactiveTrigger -> RuntimeTrigger`, including `trigger_id`, `trade_intent_id`, `trigger_source`, `triggered_at`, `onchain_checks_passed`, and the mapping for `kind/observed_out/runtime_exit_min_out` | `not assigned yet` | `before any runtime-to-execution integration claim` |
| `cli_surface` approval mapping expects camelCase mapping keys, while current compiler/reactive execution payloads are snake_case typed models | Approval rendering can break at runtime or silently omit TTL / gas / minOut fields, creating an unsafe operator view | `open` | Replace the implicit mapping contract with a frozen adapter DTO or change `build_approval_battle_card(...)` to consume typed models directly; add one real integration test using current compiler/reactive objects | `not assigned yet` | `before approving real plans through CLI` |
| Wave-level evidence itself is drifting: current `HEAD` exists, some wave2 docs remain untracked, and the prefilled W2 packet names a different module set than the completed wave2 thread docs | Reviewers can sign off the wrong module set or rely on stale git evidence, weakening auditability of the wave gate | `open` | Reconcile the authoritative W2 module list, commit the missing wave2 acceptance artifacts, and refresh thread docs that still claim `HEAD` or commit history is unavailable | `not assigned yet` | `before wave sign-off` |

## Medium Risks

| Risk | Impact | Current status | Mitigation action |
| --- | --- | --- | --- |
| `execution_compiler` and `reactive_runtime` only have schema-level overlap; direct handoff is not proven and compiler metadata fields (`compiled_at`, `compiler_version`) are absent from the reactive plan model | A direct pass-through may fail or lose metadata when teams stop using hand-written reactive test fixtures and start handing real compiler output around | `open` | Decide whether compiler metadata should survive the reactive boundary; if yes, freeze it in the reactive schema, otherwise strip it explicitly in a dedicated adapter |
| `execution_layer -> export_outputs` is currently proven only through a module test and a generic dict envelope | Field drift inside `ExecutionRecord` can still reach export without type-level rejection, especially once other producers start serializing execution data | `open` | Freeze a typed export-facing `ExecutionRecord` contract or add a cross-module contract test that fails on unexpected field changes |
| `cli_surface` route skeleton exists, but runtime dependencies (`Typer`, `Rich`) and downstream adapters are still not wired or verified end-to-end | The CLI may appear complete in docs while still failing to boot or render real approval / alert / export flows in the target runtime environment | `open` | Install runtime dependencies in the integration environment and add one CLI smoke path per major command group with explicit adapters |

## Accepted Debt

| Item | Why acceptable for now | Planned wave |
| --- | --- | --- |
| `reactive_runtime -> investment_state_machine_contract` remains at Python port/interface level rather than a real on-chain adapter | The current wave does prove the intended state-machine semantics and field mapping in unit tests, which is enough to document the seam honestly as `not verified yet` for chain execution | `Wave 3 or later` |
| `decision_context_builder` has no real downstream consumer in current wave code | This is acceptable only because the integration matrix explicitly marks the downstream seam as `not verified yet` and does not claim end-to-end consumption | `Next wave that consumes DecisionContext` |
| `execution_layer -> export_outputs` is counted only as test-level evidence, not a production integration success | The test does prove JSON serialization compatibility for the current shape, which is useful enough for documentation while keeping the remaining type-freeze risk explicit | `Wave 3 or later` |
