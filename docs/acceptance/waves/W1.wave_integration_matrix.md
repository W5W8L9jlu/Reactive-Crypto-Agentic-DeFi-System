# W1 Wave Integration Matrix

- Wave: `W1`
- Checked on: `2026-03-31`
- Branch state observed: `master`, working tree clean, repository currently has a single reachable commit `ed817b3 docs: 补充wave1线程验收与交接文档`
- Evidence inputs:
  - `docs/acceptance/threads/wave1/*.thread_acceptance.md`
  - `docs/acceptance/handoffs/wave1/*.thread_to_thread_handoff.md`
  - current code under `backend/data/providers/`, `backend/strategy/`, `backend/validation/`, `backend/export/`, `backend/contracts/`
  - `git status --short --branch`
  - `git log --oneline -12`
  - `git show --stat --summary --name-only HEAD`

## Integration Relations Checked

| Relation | What was actually checked | Evidence | Alignment observed | Mismatch / gap | Status |
| --- | --- | --- | --- | --- | --- |
| `strategy_boundary_service -> validation_engine` | Code-level schema alignment between upstream strategy models and validation input parsing was checked. `validation` imports `StrategyTemplate`, `StrategyIntent`, `TradeIntent` from `backend.strategy.models` and validates cross-object constraints plus `ExecutionPlan` hard constraints. | `backend/strategy/models.py`, `backend/validation/models.py`, `backend/validation/validation_engine.py`, `docs/acceptance/threads/wave1/strategy_boundary_service_thread_acceptance.md`, `docs/acceptance/handoffs/wave1/strategy_boundary_service_thread_to_thread_handoff.md`, `docs/acceptance/threads/wave1/validation_engine_thread_acceptance.md`, `docs/acceptance/handoffs/wave1/validation_engine_thread_to_thread_handoff.md` | Field names align for `template_id`, `template_version`, `strategy_intent_id`, `pair`, `dex`, `position_usd`, `max_slippage_bps`, `stop_loss_bps`, `take_profit_bps`, `ttl_seconds`. Unit usage also aligns here: `position_usd` is `Decimal`, `ttl_seconds` is seconds, `*_bps` are non-negative integers. | No dedicated cross-thread integration test was found. `BoundaryDecisionResult` is not consumed by `validation_engine`; only the shared domain models are aligned. | `schema-level checked; runtime flow not verified yet` |
| `provider_architecture -> wave1 downstream consumer` | Only provider-side interface shape was checked. No wave1 consumer that imports or exercises `ProviderRequest` / `ProviderResponse` was found. | `backend/data/providers/_shared_http_client.py`, `backend/data/providers/rpc_provider.py`, `backend/data/providers/graph_provider.py`, `backend/data/providers/etherscan_provider.py`, `docs/acceptance/handoffs/wave1/provider_architecture_thread_to_thread_handoff.md` | Provider contract is internally consistent: `ProviderRequest(operation, params)` -> `ProviderResponse(provider, operation, payload, metadata)`. | Handoff still lists downstream thread as `not verified yet`. No code or test evidence shows a wave1 consumer actually consuming these payloads. | `not verified yet` |
| `export_outputs <- wave1 producer threads` | Only export module input envelope was checked. No wave1 producer thread was found that passes a typed payload into `export_outputs(...)` in current code. | `backend/export/export_outputs.py`, `docs/acceptance/threads/wave1/export_outputs_thread_acceptance.md`, `docs/acceptance/handoffs/wave1/export_outputs_thread_to_thread_handoff.md` | Export surface is stable only at the envelope level: `DecisionArtifact` and `ExecutionRecord` are both `RootModel[dict[str, Any]]`, and output names are fixed as `machine_truth_json`, `audit_markdown`, `investment_memo`. | Because both inputs are generic dict wrappers, field-name drift inside `DecisionArtifact` / `ExecutionRecord` is not prevented by type checks. No upstream thread-to-export integration was evidenced. | `not verified yet` |
| `strategy_boundary_service / validation_engine -> investment_state_machine_contract` | No adapter/compiler path was found in wave1 code that translates strategy or validation outputs into on-chain `InvestmentIntent`. | `backend/strategy/models.py`, `backend/validation/models.py`, `backend/contracts/interfaces/IReactiveInvestmentCompiler.sol`, `docs/acceptance/handoffs/wave1/investment_state_machine_contract_thread_to_thread_handoff.md` | None verified end-to-end. | Upstream fields are `position_usd`, `max_slippage_bps`, `stop_loss_bps`, `take_profit_bps`, `ttl_seconds`; contract fields are `plannedEntrySize`, `entryMinOut`, `exitMinOutFloor`, plus runtime `observedOut` / `runtimeExitMinOut`. No checked mapping for field semantics, slippage conversion, or TTL handling was found. | `not verified yet` |
| `investment_state_machine_contract -> wave1 downstream consumer` | Contract interface surface was checked, but no wave1 downstream consumer was found. | `backend/contracts/interfaces/IReactiveInvestmentCompiler.sol`, `docs/acceptance/threads/wave1/investment_state_machine_contract_thread_acceptance.md`, `docs/acceptance/handoffs/wave1/investment_state_machine_contract_thread_to_thread_handoff.md` | Contract surface is clear on its own: `registerInvestmentIntent`, `executeReactiveTrigger`, `getPositionState`, `getActualPositionSize`, `getInvestmentIntent`; enum `PositionState = PendingEntry | ActivePosition | Closed`. | No compiler/reactive/execution consumer exists in this wave, so downstream integration and runtime semantics remain unverified. | `not verified yet` |

## Cross-Thread Interface Notes

### Field naming
- Checked and aligned: `strategy_boundary_service -> validation_engine` shares the same strategy domain models directly, so field names are aligned by import rather than duplicate redefinition.
- Not verified yet: `export_outputs` accepts generic dict payloads, so no field-level freeze was evidenced for `DecisionArtifact` / `ExecutionRecord`.
- Not verified yet: no checked mapping exists from strategy/validation field names to `InvestmentIntent` field names.

### Enum usage
- Checked and aligned only inside strategy boundary module: `BoundaryDecision` / `RuleDecision` stay internal to `strategy_boundary_service`.
- Not verified yet: no downstream wave1 consumer of `BoundaryDecisionResult` was found.
- Not verified yet: contract enum `PositionState` has no verified wave1 consumer.

### TTL / timestamp units
- Checked and aligned: `strategy_boundary_service` handoff documents `ttl_seconds` as seconds, and `validation_engine` enforces `ttl_seconds: PositiveInt` plus `execution_plan.hard_constraints.ttl_seconds <= trade_intent.ttl_seconds`.
- Not verified yet: no wave1 downstream consumer converts `ttl_seconds` into on-chain/runtime fields.
- No checked timestamp field was found in the verified wave1 relations above.

### BPS / percent units
- Checked and aligned: `strategy_boundary_service -> validation_engine` consistently uses integer `*_bps` fields.
- No checked `pct` field was found in the verified relation above.
- Not verified yet: no evidenced conversion from `*_bps` to contract-side `entryMinOut` / `exitMinOutFloor` semantics was found.

## Evidence Conflicts Found During Wave Check

- Multiple thread documents claim the workspace was `not a git repository` or had `no commits`, but the current branch is a valid git repository and `HEAD` resolves to `ed817b3`.
- The repository currently appears to be on its initial commit, so `HEAD^` does not exist. This means parent-based diff evidence is unavailable, but repository existence itself is confirmed.
- `investment_state_machine_contract` has duplicated acceptance/handoff file variants with different filenames:
  - `investment_state_machine_contract.thread_acceptance.md`
  - `investment_state_machine_contract_thread_acceptance.md`
  - `investment_state_machine_contract.thread_to_thread_handoff.md`
  - `investment_state_machine_contract_thread_to_thread_handoff.md`
- The duplicate files do not prove different interface shapes, but they do create ambiguity about which artifact is the canonical wave evidence.

## Wave-Level Conclusion

- Factually checked and supported by code: only the schema-level alignment between `strategy_boundary_service` and `validation_engine`.
- Factually not checked yet: provider downstream consumption, export upstream consumption, strategy/validation to contract compilation path, and contract downstream consumption.
- Wave 1 should not be described as fully integrated. At most, it has one verified schema-alignment seam plus several unverified handoff seams.
