# Prompting Recipes

## Recipe 1：新建模块骨架
适合从 0 到 1 生成文件骨架。

## Recipe 2：只实现核心函数
适合已有目录、先做 happy path。

## Recipe 3：补测试
适合实现已存在、需要验证约束。

## Recipe 4：做重构但不改行为
适合接口不变的内部重构。

每种 recipe 的模块化版本见 `docs/prompts/`。
